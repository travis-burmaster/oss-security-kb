# .NET / NuGet Index

## Seed Pages
- [[dotnet/Newtonsoft.Json]] — dominant JSON library · advisory mapped · nested-input DoS history and hardening-relevant defaults

## Future Targets
- `System.Text.Json` — modern built-in JSON stack with parser and serializer security relevance
- `Npgsql` — database driver with connection-string and protocol-surface importance
- `Serilog` — logging pipeline package with sink and formatting trust boundaries
