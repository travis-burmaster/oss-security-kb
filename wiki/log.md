## [2026-05-15] advisory-review | bleach and python-jose (PyPI), archive / sanitizer / JWT comparison checks
Ran a public-information-only review pass across Python sanitizer and JOSE / JWT gaps plus compact npm / Maven / Go comparison candidates using OSV.dev package and vulnerability records, GitHub Advisory Database records surfaced through OSV, public CVE / NVD records, PyPA advisory aliases, upstream README / security-policy / issue / PR / release references, PyPI metadata, PyPIStats where available, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260515-0305/`. Added two low-risk advisory-mapped Python pages: `bleach` now normalizes five public sanitizer-boundary records across URI-scheme filtering bypass, mutation-XSS allowlist combinations, and style-attribute ReDoS through the 3.3.0 fix boundary, while `python-jose` now captures three public JOSE records across HMAC timing comparison, OpenSSH ECDSA key algorithm confusion, and compressed-JWE resource-exhaustion behavior through the 3.4.0 fix boundary. `adm-zip`, `decompress`, `html-minifier`, `com.google.guava:guava`, and `github.com/redis/go-redis/v9` were screened as future candidates but not forced into this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-14] advisory-review | lxml (PyPI), bleach / python-jose / archive-parser comparison checks
Ran a public-information-only review pass across Python parser / sanitizer gaps and compact npm archive-parser candidates using OSV.dev package queries and vulnerability details, GitHub Advisory Database records, public CVE / NVD records, upstream lxml changelog / release references, PyPI metadata, PyPIStats, npm registry metadata for comparison candidates, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260514-1406/`. Added a new advisory-mapped page for PyPI `lxml`, normalizing seven public GHSA/CVE-backed records across recurring `lxml.html.clean` sanitizer bypasses, libxml2-backed parser denial-of-service behavior, and the 2026 `iterparse()` / `ETCompatXMLParser` XXE default-configuration fix in 6.1.0. `bleach`, `python-jose`, `adm-zip`, `crypto-js`, `decompress`, `html-minifier`, `guava`, and `go-redis` were screened as future candidates but not forced into this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-14] advisory-review | node-forge (npm), httpclient5 / bleach comparison checks
Ran a public-information-only review pass across npm cryptography / PKI, Maven HTTP-client, and Python sanitizer candidates using OSV.dev package queries and vulnerability details, GitHub Advisory Database / upstream repository advisories surfaced through OSV, public CVE / NVD records, upstream commits / PRs referenced by public advisories, npm registry metadata, npm downloads API, GitHub repository metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260514-0606/`. Added a new advisory-mapped page for npm `node-forge`, normalizing 15 public package records across certificate-chain validation, RSA / Ed25519 signature verification, ASN.1 parser / validator boundaries, BigInteger denial of service, URL parsing / open redirect behavior, and legacy prototype-pollution APIs through the `1.4.0` fix train. `org.apache.httpcomponents.client5:httpclient5`, `bleach`, `python-jose`, and other compact candidates were screened as future review options but not forced into this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-13] advisory-review | setuptools (PyPI), org.apache.zookeeper:zookeeper (Maven), glob (npm)
Ran a public-information-only review pass across Python packaging, Maven coordination-service, and npm globbing candidates using OSV.dev package queries and vulnerability details, GitHub Advisory Database / upstream GHSA records surfaced through OSV, public CVE/NVD records, upstream fix commits / release notes / Apache advisory-list references, registry metadata, npm downloads metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260513-2206/`. Added three low-risk advisory-mapped pages: `setuptools` now normalizes four public package-index / transport records through the 78.1.1 path-traversal fix, `org.apache.zookeeper:zookeeper` now captures nine public records across quorum authorization, AdminServer permission / IP-authentication boundaries, persistent watcher disclosure, TLS reverse-DNS hostname verification, configuration logging exposure, and command DoS through 3.9.5, and `glob` now captures the CLI-specific `-c` / `--cmd` command-injection advisory fixed in 10.5.0 / 11.1.0. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public evidence.

## [2026-05-13] advisory-review | pip (PyPI), setuptools / MarkupSafe comparison checks
Ran a public-information-only review pass across Python packaging gaps, with PyPI `pip` selected as the substantive update because it had no dedicated KB page while current public OSV / GHSA records show 11 normalized package-scoped vulnerability groups. Evidence was saved under `raw/advisory-review-20260513-1406/` from OSV.dev package and vulnerability records, PyPA advisory records surfaced through OSV, public CVE/NVD records, the upstream pip changelog and SECURITY.md, Python security-announcement references surfaced through OSV, PyPI metadata, and PyPIStats download metadata. The new Python page maps archive-extraction and path-traversal issues, VCS reference / Mercurial option injection, installer import-order behavior, and legacy transport / temporary-directory hardening through the 26.1 fix train. `setuptools` and `MarkupSafe` were screened as comparison candidates; `setuptools` remains a strong future packaging-tool page, while `MarkupSafe` still returned no package-scoped OSV records in this pass. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-13] advisory-review | paramiko (PyPI), httpx / commons-lang3 / zookeeper comparison checks
Ran a public-information-only review across Python, Maven, and npm gaps, with PyPI `paramiko` selected as the substantive update because it had no dedicated KB page while OSV / GHSA records showed six normalized public vulnerability groups. Evidence was saved under `raw/advisory-review-20260513-0205/` from OSV.dev, GitHub Advisory Database, public CVE records, the upstream Paramiko changelog, PyPI metadata, the repository security-policy page, and the public OSTIF audit reference for CVE-2026-44405. The new page maps server-mode authentication bypasses, a local private-key file race, SSH Terrapin protocol mitigation, legacy randomness, and the recent SHA-1 algorithm record without adding speculative exploit claims. `httpx`, `org.apache.commons:commons-lang3`, `org.apache.zookeeper:zookeeper`, and `passport` were screened as comparison candidates; `passport` already had a page, while the others remain good future compact pages. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-13] advisory-review | org.apache.httpcomponents:httpclient (Maven), commons-lang3 / httpx / passport comparison checks
Ran a public-information-only review pass across Maven, Python, and npm gaps, with `org.apache.httpcomponents:httpclient` selected as the substantive update because it had no dedicated Maven page while current public OSV / GHSA records show 6 package-scoped advisories. Evidence was saved under `raw/advisory-review-20260513-0305/` from OSV.dev package and vulnerability records, GitHub Advisory Database aliases surfaced through OSV, public CVE records, Apache HttpComponents release / security pages, Maven Central metadata, and upstream issue / commit references surfaced by the public records. The new Maven page maps TLS hostname-verification failures, authenticating-proxy credential leakage, SSL-handshake timeout denial of service, and malformed-URI target-host confusion through the 4.5.13 / 5.0.3 fix boundary. `org.apache.commons:commons-lang3`, PyPI `httpx`, and npm `passport` were screened as comparison candidates but not forced because `httpclient` carried the clearer high-blast-radius Java HTTP-client gap. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-12] agent-audit-mvp | semver (npm)
Ran the first source-oriented Agent Audit MVP pass against npm `semver` / `npm/node-semver` after generating the new intake workflow. Reviewed current upstream `v7.8.0` at commit `efa4be6096c1f9b77d9d27d6132f6220c43b4e31`, focusing on parser-complexity and ReDoS surfaces tied to `GHSA-c2qf-rxjj-qqgw` / `CVE-2022-25883` and `GHSA-x6fg-f45m-jf5q` / `CVE-2015-8855`. Evidence came from upstream `SECURITY.md`, `internal/re.js`, `classes/range.js`, `classes/semver.js`, `test/integration/whitespace.js`, a clean `npm test` run, a small local pathological-input timing probe, `npm audit --omit=dev`, npm registry/download metadata, and OSV/GHSA advisory records. No maintainer escalation was opened because no minimal failing repro or current security defect was found; the KB page now records this as KB-only audit coverage and notes the remaining value of broader reusable fuzzing.

## [2026-05-12] advisory-review | protobufjs (npm), httpx and commons-lang3 comparison checks
Ran a public-information-only review pass across npm / Python / Maven gaps, with `protobufjs` selected as the substantive update because it had no dedicated npm page while current public OSV / GHSA records show 11 package-scoped advisories and npm reported roughly 62.1M weekly downloads. Evidence was saved under `raw/advisory-review-20260512-1405/` from OSV.dev package and vulnerability records, GitHub Advisory Database records, public CVE aliases, upstream SECURITY.md and GitHub release notes, npm registry metadata, and npm downloads API. The new npm page maps prototype-pollution / prototype-injection history, generated-code code-injection risks, overlong UTF-8 decoding, parser / recursion / option-path denial-of-service issues, and the older regex DoS record through the 7.5.6 / 8.0.2 fix train, while noting that newer npm releases exist beyond the public fixed-version boundary. `httpx` and `org.apache.commons:commons-lang3` were screened as comparison candidates but not forced into this pass because `protobufjs` carried the clearer high-impact gap. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-11] advisory-review | org.springframework.security:spring-security-core (Maven), spring-webflux and netty-codec-http comparison checks
Ran a public-information-only review pass across Maven security-framework / HTTP-stack gaps, with `org.springframework.security:spring-security-core` selected as the substantive update because it had no dedicated package page while public OSV / GHSA records show 31 package-scoped advisories. Evidence was saved under `raw/advisory-review-20260511-2205/` from OSV.dev package and vulnerability records, GitHub Advisory Database aliases surfaced through OSV, public CVE/NVD records, Spring security advisory / commit references surfaced through OSV, and Maven Central metadata. The new Maven page maps authorization and authentication bypasses, method-security annotation boundaries, cryptographic / token-generation weaknesses, deserialization, security-context behavior, and DoS / enumeration issues through the 6.5.10 / 7.0.5 fix train. `org.springframework:spring-webflux` and `io.netty:netty-codec-http` were screened as future-review candidates rather than forcing rushed pages here. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-11] advisory-review | org.bouncycastle:bcpkix-jdk18on (Maven), spring-webflux and spring-security-core comparison checks
Ran a public-information-only review pass across Maven crypto / web-framework gaps, with `org.bouncycastle:bcpkix-jdk18on` selected as the substantive update because it had no dedicated package page while public OSV / GHSA records show 3 package-scoped advisories. Evidence was saved under `raw/advisory-review-20260511-1805/` from OSV.dev package and vulnerability records, GitHub Advisory Database aliases surfaced through OSV, public CVE/NVD records, Bouncy Castle CVE wiki / commit references surfaced through OSV, and Maven Central metadata. The new Maven page maps PEM / ASN.1 denial of service, PKIX certificate-path excessive allocation, and composite-signature validation behavior through the `1.84` fix boundary; the master index also now includes the existing Spring Security Maven pages that had been present in the section index but omitted from the root Maven list. `org.springframework:spring-webflux` and `org.springframework.security:spring-security-core` were screened as future-review candidates rather than forcing rushed pages here. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-11] advisory-review | org.springframework:spring-web (Maven), spring-webflux and bcpkix comparison checks
Ran a public-information-only review pass across Maven web / crypto gaps, with `org.springframework:spring-web` selected as the substantive update because it had no dedicated package page while current public OSV / GHSA records show 12 package-scoped advisories. Evidence was saved under `raw/advisory-review-20260511-1005/` from OSV.dev package and vulnerability records, GitHub Advisory Database aliases surfaced through OSV, public CVE/NVD aliases, Spring security advisory / issue / commit references surfaced through OSV, and Maven Central metadata. The new Maven page maps URL parsing / host validation, reflected file download, conditional-request DoS, DataBinder behavior, unsafe deserialization, XML input DoS, Cross-Site Tracing, CSRF, privilege-management, and XSS / response-generation boundaries through `CVE-2025-41234`. `org.springframework:spring-webflux` and `org.bouncycastle:bcpkix-jdk18on` were screened as future-review candidates rather than forcing rushed pages here. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-10] advisory-review | tornado (PyPI), gunicorn and Spring web comparison checks
Ran a public-information-only review pass across Python and Maven web-framework gaps, with `tornado` selected as the substantive update because it had no Python package page while current public OSV / GHSA records show 14 package-scoped records and roughly 28.1M weekly PyPI downloads. Evidence was saved under `raw/advisory-review-20260510-1005/` from OSV.dev package and vulnerability records, GitHub Advisory Database / GitHub Security Advisories surfaced through OSV, public CVE/NVD aliases, upstream Tornado security-policy / release pages, PyPI metadata, and PyPIStats. The new Python page maps representative public vulnerability classes across HTTP request smuggling, cookie and multipart denial of service, CRLF / cookie-attribute injection, open redirect, and legacy XSRF side-channel behavior through `6.5.5`. `gunicorn`, `org.springframework:spring-web`, `org.springframework:spring-webmvc`, `org.yaml:snakeyaml`, and `org.bouncycastle:bcprov-jdk18on` were screened as rich future-review candidates rather than forcing rushed pages here. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-10] advisory-review | brace-expansion (npm), glob / archive parser gaps
Ran a public-information-only review pass across npm parser / globbing-adjacent gaps, with `brace-expansion` selected as the substantive update because it had no package page while current public OSV / GHSA records show three direct package-scoped advisories and roughly 479.7M weekly npm downloads. Evidence was saved under `raw/advisory-review-20260510-0605/` from OSV.dev package and vulnerability records, GitHub Advisory Database / GHSA API records, public CVE/NVD records, upstream issues / PRs / commits, npm registry metadata, and npm downloads API. The new npm page maps the 2017 ReDoS issue, the 2025 multi-major ReDoS fix train, and the 2026 zero-step sequence hang / memory-exhaustion advisory fixed across maintained major lines through `5.0.5`. Comparison gaps such as `glob`, `adm-zip`, `decompress`, and `yauzl` remain worth future review rather than forcing rushed pages here. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-10] advisory-review | org.apache.tomcat.embed:tomcat-embed-core (Maven), django (PyPI)
Ran a public-information-only review pass across Maven and Python high-blast-radius candidates, with `org.apache.tomcat.embed:tomcat-embed-core` selected as the substantive update after the prior run flagged its large public OSV history. Evidence was saved under the latest `raw/advisory-review-20260510-*` bundle from OSV.dev package queries, GitHub Advisory Database aliases surfaced through OSV, public CVE/NVD references, Apache Tomcat security pages, and Maven Central metadata. The new Maven page maps 64 public package-scoped OSV records at a representative-class level across HTTP parsing / request validation, HTTP/2 and multipart denial of service, security-constraint and CLIENT_CERT authentication bypasses, open redirects, logging / information disclosure, and path/resource boundary issues through 2026. `django` was screened as a comparison candidate and remains a large already-represented Python page rather than a forced update in this pass. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-09] advisory-review | Twisted (PyPI), tomcat-embed-core (Maven), spring-core (Maven)
Ran a public-information-only review pass across three candidates using OSV.dev package queries, GitHub Advisory Database / GitHub Security Advisory records surfaced through OSV, public CVE/NVD aliases, upstream Twisted SECURITY.md and NEWS.rst, PyPI metadata, PyPIStats downloads, and Maven comparison records collected under `raw/advisory-review-20260509-1805/`. `Twisted` was the substantive update from this pass, so a new advisory-mapped Python page now captures representative public vulnerability classes across HTTP request-smuggling / parser ambiguity, HTTP pipelining response disordering, TLS validation, CRLF / header injection, redirect/header exposure, HTML injection, SSH / DNS / HTTP2 denial of service, and reverse-proxy forced-browsing behavior through `CVE-2026-42304` / `GHSA-grgv-6hw6-v9g4`. `org.apache.tomcat.embed:tomcat-embed-core` surfaced a large 64-record public OSV history and should receive a dedicated Maven pass rather than a rushed partial page; `org.springframework:spring-core` is already represented in the KB. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence.


## [2026-05-09] advisory-review | org.springframework:spring-core (Maven), MarkupSafe (PyPI), lodash-es (npm)
Ran a public-information-only review pass across three candidates using OSV.dev package queries and vulnerability records, GitHub Advisory Database aliases surfaced through OSV, public CVE/NVD records, Spring advisory / release links surfaced from OSV, and Maven Central metadata collected under `raw/advisory-review-20260509-0605/`. `org.springframework:spring-core` was the substantive update from this pass, so a new advisory-mapped Maven page now captures 18 public records across static-resource / path normalization, Spring Framework + Spring Security authorization boundaries, annotation / method-security resolution, logging output neutralization, optional JSONP behavior, remoting/deserialization, and web denial-of-service cases through `CVE-2025-41249` / `GHSA-jmp9-x22r-554x`. `MarkupSafe` returned no package-scoped OSV records in this pass, and `lodash-es` surfaced lodash-family records already represented by the existing lodash page, so no filler pages were forced. The required local Claude-compatible proxy at `http://127.0.0.1:8319` was attempted twice but returned HTTP 429 rate-limit responses, so proxy synthesis was not used; final edits were curated manually from saved public evidence.

## [2026-05-08] advisory-review | hyper (Rust / crates.io)
Ran a public-information-only review pass across Rust ecosystem gaps, with `hyper` selected as the substantive high-blast-radius HTTP-stack target after screening `Django`, `org.springframework:spring-core`, `github.com/stretchr/testify`, `hyper`, and `twisted` as candidates. Evidence was saved under `raw/advisory-review-20260508-2305/` from OSV.dev package and vulnerability records, GitHub Advisory Database / GitHub Security Advisory links surfaced through OSV, RustSec records, public CVE / NVD aliases, crates.io metadata, and upstream Hyper security-policy / changelog references. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence. Added `wiki/rust/hyper.md` covering the public HTTP/1 request-smuggling / parser-differential cluster, header injection, Windows TLS hostname-verification issue, and parser soundness advisory; updated Rust and master indexes.

## [2026-05-08] advisory-review | sudo (Linux)
Ran a public-information-only review pass across Linux future targets (`sudo`, `openssh`, and Homebrew/Linux transport-adjacent `curl` context), with `sudo` selected as the substantive privilege-boundary gap. Evidence was saved under `raw/advisory-review-20260508-0605/` from upstream Sudo advisories and release notes, OSV.dev records, the public GitHub Advisory Database, and public CVE/NVD records. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence and kept to advisory-level facts with no exploit steps. Added `wiki/linux/sudo.md` covering CVE-2019-18634, CVE-2021-3156 / GHSA-w5vh-2923-gp5c, CVE-2025-32462, and CVE-2025-32463, updated Linux and master indexes, and preserved distro-backport caveats for future normalization.

## [2026-05-06] advisory-review | System.Security.Cryptography.Xml (NuGet), telnyx (PyPI), merge (npm)
Ran a public-information-only drift check across two short package pages and one already-mapped npm comparison candidate using OSV.dev package queries, public GitHub Advisory Database / GitHub Security Advisory records surfaced from OSV, dotnet/announcements, public CVE / MSRC / NVD references, the PyPA advisory database / PyPI incident report, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260506-1405/`. `System.Security.Cryptography.Xml` was the substantive update: its page now reflects all five package-scoped OSV records, adding the 2018 XML-processing DoS pair, the 2022 information-disclosure package boundary, and the second April 2026 EncryptedXml DoS advisory while keeping fixed versions tied to public records. `telnyx` received a small incident-identifier reconciliation so `PYSEC-2026-3`, `GHSA-955r-262c-33jc`, and `MAL-2026-2254` are treated as parallel identifiers for the same malicious PyPI release incident rather than separate findings. `merge` was screened as a comparison candidate and remained aligned with its two public prototype-pollution advisories, so no filler edit was forced. The proxy synthesis step succeeded and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle.

## [2026-05-05] advisory-review | axios (npm), marked (npm), mathjs/basic-ftp/uuid comparison checks
Ran a public-information-only advisory refresh across one high-download npm page and several smaller drift candidates using OSV package queries and vulnerability records, GitHub Advisory Database links present in OSV, public CVE/NVD aliases, npm registry/download metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260505-1439/`. `axios` was the substantive update: its page now reflects 25 public OSV/GHSA records and adds the newly published May 2026 cluster covering prototype-pollution read-side gadgets, header / XSRF / JSON response tampering paths, proxy-bypass follow-ons, streamed upload/response size-limit bypasses, multipart CRLF injection, null-byte injection, and `toFormData` recursion DoS. `marked`, `mathjs`, `basic-ftp`, and `uuid` were screened as comparison candidates; their small identifier drifts were not forced into this pass because axios carried the clear high-impact maintenance update. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final axios edits were checked back against the saved public evidence bundle before landing.
## [2026-05-05] advisory-review | org.apache.commons:commons-compress (Maven)
Ran a public-information-only review pass across Maven package gaps, with `org.apache.commons:commons-compress` selected as the clean substantive update. Evidence was saved under `raw/advisory-review-20260505-1537/` from OSV.dev package and vulnerability records, the public GitHub Advisory Database aliases referenced by OSV, Apache Commons Compress security documentation, public CVE aliases, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`. The proxy synthesis step succeeded and was used only as a drafting aid; final edits were checked back against saved public evidence. Added a new advisory-mapped Maven package page covering 11 public denial-of-service / resource-exhaustion advisories across DUMP, Pack200, TAR, ZIP, 7Z, and BZip2 archive/compression surfaces, updated the Maven index, and repaired root-index omissions for existing evidence-backed pages (`minimatch`, `merge`, `github.com/gorilla/schema`, and the Linux Copy Fail note) so navigation/counts match the checked-in wiki pages.

## 2026-05-04 — React Server Components package gap: react-server-dom-webpack

## 2026-05-04 — npm next advisory refresh

Ran a public-information-only review pass against the high-value npm `next` package after detecting that the KB page only contained one advisory while the current OSV.dev package query returned 42 package-scoped records. Evidence was collected under `raw/advisory-review-20260504-2239/` from OSV.dev package and vulnerability records, GitHub Advisory Database public records, public CVE aliases, npm registry metadata, and npm downloads API. The required local Claude-compatible proxy at `http://127.0.0.1:8319` succeeded and was used only as a drafting aid. The `next` page was expanded into a fuller advisory-mapped page covering the public trail across legacy traversal / XSS issues, image optimizer flaws, cache poisoning, middleware authorization bypasses, Server Actions / Server Components issues, request smuggling, SSRF, RCE, source exposure, and 2026 DoS / CSRF fixes. Updated the npm and root indexes to describe the broader advisory surface rather than only the earlier App Router DoS entry.


Ran a public-information-only review pass across the React / Next.js Server Components surface using OSV.dev package queries for `react-server-dom-webpack`, `react-dom`, and `next`, GitHub Advisory Database / React Security Advisory records, public CVE / NVD aliases, the React RSC disclosure blog, npm downloads API data, and the required local Claude-compatible proxy collected under `raw/advisory-review-20260504-183750/`. `react-server-dom-webpack` was the clean substantive update from this pass, so a new advisory-mapped page now captures the six published package-scoped RSC advisories across unauthenticated RCE (`CVE-2025-55182`), source-code exposure (`CVE-2025-55183`), and repeated denial-of-service fix trains through `19.0.5`, `19.1.6`, and `19.2.5`. `react-dom` and `next` were reviewed as comparison candidates: `react-dom` surfaced only its legacy `CVE-2018-6341` package record, while `next` showed a much larger direct OSV surface that should get a dedicated normalization pass rather than a rushed partial update. The proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against saved public evidence before landing.

## [2026-05-04] advisory-review | elliptic (npm), prismjs (npm)
Ran a public-information-only review pass across two high-volume npm package gaps using OSV.dev package queries, detailed OSV vulnerability records, GitHub Advisory Database / repository security advisories, public CVE records, upstream issues / PRs / commits, npm registry metadata, npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260504-1438/`. Both candidates became substantive KB additions: `elliptic` now captures nine published records across ECDSA / EDDSA signature malleability, ECDH point-validation risk, valid-signature rejection, a critical malformed-input ECDSA private-key extraction issue fixed in `6.6.1`, and the currently unfixed public `CVE-2025-14505` deterministic-`k` advisory; `prismjs` now captures six published records across plugin-specific XSS, grammar / comment ReDoS, and DOM-clobbering XSS fixed through `1.30.0`. The proxy synthesis step succeeded and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-05-04] advisory-review | handlebars (npm), next (npm)
Ran a public-information-only OSV cross-check across existing npm package pages and found two packages with advisory identifiers not yet reflected verbatim in their KB pages. `handlebars` received the substantive update from this pass: the page now adds CVE-2019-20920 / GHSA-3cqr-58rm-57f8 for the 2019 `lookup`-helper arbitrary-code-execution lineage fixed in `3.0.8` / `4.5.3`, and notes GHSA-f52g-6jhx-586p as an OSV duplicate/parallel record for the existing `4.4.5` raw-block parsing / ReDoS fix. Evidence was saved under `raw/advisory-review-20260504-1040/` from OSV.dev, the public CVE record, and upstream release notes. `next` surfaced many missing identifiers in OSV, but it was deferred because the page needs a broader framework-mode normalization pass rather than a rushed partial edit. The required local Claude-compatible proxy synthesis step succeeded and was used only as a drafting aid; final Handlebars edits were checked back against the saved public evidence.

## [2026-05-02] advisory-review | cors (npm), helmet (npm), cookie-parser (npm)
Ran a public-information-only triage pass across three high-usage Express middleware packages using OSV.dev package queries (direct `api.osv.dev/v1/query` calls), with evidence saved under `raw/advisory-review-20260502-033857/`. All three packages (`cors`, `helmet`, `cookie-parser`) returned **no package-scoped OSV records** in this pass, so no KB page updates were forced. Note: the OSV `/v1/search` endpoint returned `404` in this environment during this run, so this pass intentionally relied only on direct package queries rather than broader keyword enumeration.

## [2026-04-25] advisory-review | ajv (npm), async (npm), graphql (npm)
Ran a public-information-only review pass across three high-usage npm package gaps using OSV package queries, detailed OSV vulnerability records, GitHub Advisory Database pages, public CVE / NVD records, upstream release notes / changelog material, upstream issue and fix-reference links, npm registry metadata, npm downloads API, GitHub repository metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260425-063841/`. All three candidates turned into clean substantive additions from this pass, so new advisory-mapped pages now capture `ajv`'s published prototype-pollution and `$data`-driven ReDoS history across the `6.x` and `8.x` lines, `async`'s `mapValues()` / iterator-path prototype-pollution fix in `2.6.4` and `3.2.2`, and `graphql`'s 2023 overlapping-fields resource-exhaustion issue fixed in `16.8.1`. The proxy synthesis step succeeded and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-24] advisory-review | tar-fs (npm), xmldom (npm), vue (npm)
Ran a public-information-only review pass across one clean archive-extraction gap and two comparison candidates using OSV package queries, GitHub Advisory Database / public GitHub security advisories, public CVE records, maintainer fix commits, Debian LTS advisories, npm registry metadata, npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260424-1437/`. `tar-fs` was the only substantive KB update from this pass, so a new advisory-mapped page now captures its four currently published package records across the older hardlink-based overwrite flaw and the later 2024-2025 link-following, extraction-escape, and symlink-validation-bypass fixes that landed across the `1.x`, `2.x`, and `3.x` lines through `3.1.1`. `xmldom` and `vue` were both re-screened as comparison candidates in the same window, but no page changes were forced because `xmldom`'s now-broader 2026 XML serialization / injection history deserves a more careful dedicated normalization pass, while `vue` still surfaced only one odd thinly corroborated package-level record and did not justify a rushed page. The proxy synthesis step succeeded and was used only as a drafting aid; final `tar-fs` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-24] advisory-review | react (npm), zod (npm), vue (npm)
Ran a public-information-only review pass across two clean npm package gaps and one lower-confidence comparison candidate using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream release / blog references, repository security-policy checks, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260424-153828/`. `react` and `zod` were both substantive KB additions from this pass, so new advisory-mapped pages now capture React's two legacy pre-`1.0` XSS records and Zod's single published email-validation ReDoS / denial-of-service issue fixed in `3.22.3`, while keeping the important scope limit clear that this pass did not surface newer direct package-level OSV / GHSA records for modern React. `vue` was screened again as a comparison candidate in the same window, but its lone public package advisory still presents an odd affected-range story and thin upstream corroboration, so no page was forced from this pass. The proxy synthesis step succeeded and was used only as a drafting aid; final `react` and `zod` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-24] advisory-review | highlight.js (npm), zod (npm), vue (npm)
Ran a public-information-only review pass across one clean npm rendering gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream fix PR / commit references, upstream release notes, npm registry metadata, npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260424-0637/`. `highlight.js` was the only substantive KB update from this pass, so a new advisory-mapped page now captures its public package history across the 2020 crafted-language-name prototype-pollution issue fixed on the `9.x` and `10.x` lines plus the later grammar-driven ReDoS / freeze issue fixed in `10.4.1`, while keeping the crucial scope limits clear that both advisories primarily matter when applications highlight attacker-controlled content. `zod` and `vue` were re-screened as comparison candidates in the same window, but no page changes were forced because `zod`'s single older email-validation DoS record looked lower-leverage than the cleaner `highlight.js` gap for this pass, and `vue`'s lone public OSV package record still presents an odd pre-`3.0.0` affected range that deserves a more careful framework-history check rather than a rushed partial page. The proxy synthesis step succeeded and was used only as a drafting aid; final `highlight.js` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-23] advisory-review | pug (npm), github.com/go-chi/chi (Go), vue (npm)
Ran a public-information-only review pass across two clean package gaps and one lower-confidence comparison candidate using OSV package queries, GitHub Advisory Database / public GHSA pages, public CVE records, upstream `SECURITY.md`, upstream release notes / advisory text, npm registry metadata, the npm downloads API, Go module tag metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260423-1837/`. `pug` and `github.com/go-chi/chi` were both substantive KB additions from this pass, so new advisory-mapped pages now capture `pug`'s two public compiler-option code-execution fixes in `3.0.1` and `3.0.3`, plus `chi`'s `RedirectSlashes` open-redirect history including the later backslash-based follow-on fixed in `v5.2.4` after the earlier host-header issue. `vue` was re-screened as a comparison candidate in the same window, but its single low-severity public package advisory surfaced an odd pre-`3.0.0` affected range and weak modern fix-version clarity, so no page was forced from this pass. The proxy synthesis step succeeded and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-23] advisory-review | dompurify (npm), react-router (npm), markupsafe (PyPI)
Ran a public-information-only review pass across one high-value sanitizer gap and two comparison candidates using OSV package queries, OSV vulnerability records, GitHub Advisory Database / public GitHub security advisories, public CVE records, upstream release notes, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260423-1137/`. `dompurify` was the only clean substantive KB update from this pass, so a new advisory-mapped page now captures its dense public package security history across repeated mutation-XSS / sanitization-bypass fixes, prototype-pollution-sensitive configuration flaws, and the recent `3.3.2` plus `3.4.0` hardening train, while also recording that current npm metadata now shows `3.4.1` beyond the present public fix point. `react-router` and `markupsafe` were re-screened as comparison candidates in the same window, but no page changes were forced because the former still looked narrower and more mode-specific than this maintenance pass justified, while the latter did not surface a package-scoped OSV record. The proxy synthesis step succeeded and was used only as a drafting aid; final `dompurify` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-23] advisory-review | react-router (npm), dompurify (npm), markupsafe (PyPI)
Ran a public-information-only review pass across one previously deferred but now clean npm routing gap and two comparison candidates using the GitHub Advisory Database / public GitHub security advisories, OSV.dev vulnerability records, public CVE records, upstream `SECURITY.md`, upstream changelog / release notes, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260423-1539/` and the earlier comparison bundle under `raw/advisory-review-20260423-1137/`. `react-router` was the only substantive KB update from this pass, so a new advisory-mapped page now captures seven published package advisories across the 6.x and 7.x lines while preserving the crucial public scope distinctions between Framework Mode, Data Mode, Declarative Mode, and unstable RSC surfaces instead of flattening them into misleading package-wide claims. `dompurify` and `markupsafe` were re-screened as comparison candidates in the same maintenance window, but no further edits were warranted because the former had already landed cleanly on `main` and the latter still did not surface a stronger package-scoped public advisory basis than the conservative evidence already on hand. The proxy synthesis step succeeded and was used only as a drafting aid; final `react-router` edits were checked back against saved public advisory, changelog, and metadata evidence before landing.

## [2026-04-23] advisory-review | express-session (npm), pyyaml (PyPI), helmet (npm)
Ran a public-information-only maintenance pass across two existing package pages that looked underrepresented in the KB navigation plus one conservative comparison candidate using OSV package queries, the existing evidence-backed package pages, repository index consistency checks, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with drafting artifacts saved under `raw/advisory-review-20260423-0737/`. `express-session` and `pyyaml` did not need new package-page vulnerability claims in this pass, but they were confirmed as clean, evidence-backed additions to the master index, which had been omitting both existing pages and therefore undercounting the KB by two packages; the master index now includes concise entries for both, with totals corrected to 87 tracked packages and 5 Python / PyPI entries. `helmet` was re-screened as a comparison candidate and again did not justify a page change because its package-scoped OSV query remained empty. The proxy synthesis step succeeded and was used only as a drafting aid for the short master-index blurbs; final edits were kept strictly additive and checked back against the existing package pages and saved public-query output before landing.

## [2026-04-22] advisory-review | http-proxy (npm), helmet (npm), cookie-parser (npm), github.com/gorilla/mux (Go)
Ran a public-information-only review pass across one clean npm proxy gap and three conservative comparison candidates using OSV package queries, detailed OSV vulnerability records, public GitHub Advisory Database / GHSA pages, the public NVD record for `CVE-2017-16014`, upstream changelog material, npm registry metadata, the npm downloads API, public repository metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260423-0337/`. `http-proxy` was the only substantive KB update from this pass, so a new advisory-mapped page now captures its two currently published direct package-level denial-of-service records: the older insufficient-error-handling issue `GHSA-9xw9-pvgv-6p76` / `CVE-2017-16014` fixed in `0.7.0`, and the later long-request-body crash `GHSA-6x33-pw7p-hmpq` fixed in `1.18.1`, with the public changelog explicitly tying the latter fix to upstream PR `#1447`. `helmet`, `cookie-parser`, and `github.com/gorilla/mux` were re-screened as comparison candidates in the same maintenance window, but no page changes were forced because their package-scoped OSV queries remained empty and this pass did not surface stronger direct package-level advisory evidence than their current conservative baseline pages already carry. The proxy synthesis step succeeded and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-22] advisory-review | pyyaml (PyPI), markupsafe (PyPI), dompurify (npm)
Ran a public-information-only review pass across one high-value Python parser gap and two comparison candidates using OSV package queries, GitHub Advisory Database / PyPA advisory aliases, public CVE records, upstream `CHANGES`, PyPI metadata, PyPIStats, the upstream security-policy page, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260422-1837/`. `pyyaml` was the only clean substantive KB update from this pass, so a new advisory-mapped page now captures its four currently published package records across the long-running unsafe-deserialization / arbitrary-code-execution fix train from `CVE-2017-18342` through the `5.4` follow-on hardening release. `markupsafe` and `dompurify` were re-screened as comparison candidates in the same maintenance window, but no page changes were forced because the former did not surface a direct package-scoped OSV record in this quick pass and the latter still looked better suited to a wider normalization pass than a rushed partial summary. The proxy synthesis step succeeded and was used only as a drafting aid; final `pyyaml` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-22] advisory-review | sanitize-html (npm), react-router (npm), dompurify (npm)
Ran a public-information-only review pass across one substantive HTML-sanitization gap and two comparison candidates using OSV package queries, GitHub Advisory Database / public GitHub security advisories, public CVE records, upstream issue / PR / commit references, npm registry metadata, npm downloads API, GitHub repository metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260422-143824/sanitize-html/`. `sanitize-html` was the only clean substantive KB update from this pass, so a new advisory-mapped page now captures its nine currently published package records across recurring XSS / sanitization-bypass, input-validation, ReDoS, information-exposure, and the narrowly scoped 2026 `allowedTags` / `nonTextTags` regression fixed in `2.17.3`. `react-router` and `dompurify` were re-screened as comparison candidates in the same maintenance window, but both still surfaced broader, mode-sensitive client / framework histories than this compact pass could normalize safely without risking a rushed summary, so no speculative page changes were forced there. The proxy synthesis step succeeded and was used only as a drafting aid; final `sanitize-html` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-22] advisory-review | sharp (npm), sanitize-html (npm), react-router (npm)
Ran a public-information-only review pass across one high-value native-module gap and two comparison candidates using OSV package queries, GitHub Advisory Database / upstream GitHub security advisories, public CVE records, upstream SECURITY.md, fix commits, npm registry metadata, npm downloads API, GitHub tag metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260422-103819/`. `sharp` was the only clean substantive update from this pass, so a new advisory-mapped page now captures its currently published package security history across the 2022 install-time `PKG_CONFIG_PATH` command-injection issue fixed in `0.30.5` and the 2023 `libwebp`-backed WebP decoding exposure fixed in `0.32.6`. `sanitize-html` and `react-router` were reviewed as comparison candidates in the same pass, but both surfaced larger multi-advisory application-mode / configuration-sensitive histories than this maintenance window could normalize safely without risking a rushed summary, so no speculative changes were forced there. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `sharp` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-22] advisory-review | github.com/gorilla/websocket (Go), github.com/gorilla/mux (Go), serde_json (crates.io)
Ran a public-information-only review pass across one clean Go networking gap and two conservative comparison candidates using OSV package queries, the Go vulnerability record, the public GitHub Advisory Database, the public CVE / NVD record for `CVE-2020-27813`, upstream fix PR / commit / release metadata, public repository metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260422-0637/gorilla-websocket/`. `github.com/gorilla/websocket` was the only substantive KB update from this pass, so a new advisory-mapped page now captures its single currently published package issue: the integer-overflow / read-limit bypass denial-of-service flaw fixed in `v1.4.1`, while explicitly noting that the two OSV package results (`GHSA-3xh2-74w9-5vxm` and `GO-2020-0019`) collapse to the same underlying vulnerability rather than two separate bugs. `github.com/gorilla/mux` and `serde_json` were re-screened as comparison candidates in the same maintenance window, but no page changes were forced because `gorilla/mux` still did not surface a clean package-scoped OSV / GHSA entry and the OSV package query for `serde_json` remained empty. The proxy synthesis step succeeded and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-22] advisory-review | loader-utils (npm), nth-check (npm), y18n (npm)
Ran a public-information-only review pass across three npm package gaps using OSV package queries, GitHub Advisory Database records, public upstream issue / PR / commit references, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260422-023935/`. All three candidates turned into clean substantive additions from this pass, so new advisory-mapped pages now capture `loader-utils`'s 2022 ReDoS pair plus the same-year prototype-pollution issue across the 1.x, 2.x, and 3.x lines, `nth-check`'s pre-`2.0.1` regex-complexity denial-of-service issue fixed by replacing the parser regex with a hand-rolled parser, and `y18n`'s prototype-pollution fix train coordinated across the `3.x`, `4.x`, and `5.x` lines. The proxy synthesis step succeeded and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-22] advisory-review | path-parse (npm), minimist (npm), semver (npm)
Ran a public-information-only review pass across one clean npm package gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public upstream issue / PR / fix-commit references, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260422-033814/`. `path-parse` was the only substantive KB update from this pass, so a new advisory-mapped page now captures its single currently published package record: the pre-`1.0.7` regular-expression denial of service issue tracked as `GHSA-hj48-42vr-x3v9` / `CVE-2021-23343`, with the public evidence chain preserved through upstream issue `#8`, PR `#10`, and merge commit `eca63a7` that replaced the older regex structure. `minimist` and `semver` were re-screened as comparison candidates in the same maintenance window, but their existing pages already matched the currently published GHSA / OSV package history, so no filler edits were forced. The proxy synthesis step succeeded and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-21] advisory-review | pac-resolver (npm), github.com/gorilla/mux (Go), openssl@3 (Homebrew)
Ran a public-information-only review pass across one clean npm package gap and two conservative comparison candidates using OSV package queries, the public GitHub Advisory Database, the public CVE / NVD record for `CVE-2021-23406`, upstream release notes, npm registry metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260421-103744/`. `pac-resolver` was the only substantive KB update from this pass, so a new advisory-mapped page now captures its single currently published package record: pre-`5.0.0` code injection from unsafe PAC-file handling, with the public advisory trail explicitly noting that the effective fix landed through the maintainer-controlled `degenerator` dependency and the `5.0.0` release notes confirming the sandboxing change. `github.com/gorilla/mux` and `openssl@3` were re-screened in the same maintenance window as comparison candidates and again did not justify page changes from the compact public evidence reviewed here. The proxy synthesis step succeeded and was used only as a drafting aid; final KB edits were checked back against the saved public evidence before landing.

## [2026-04-20] advisory-review | ip (npm), shell-quote (npm), passport (npm)
Ran a public-information-only review pass across three npm package gaps using OSV package queries, detailed OSV vulnerability records, public GitHub Advisory Database records via GHSA references, public CVE / NVD aliases, upstream issue / PR / commit and changelog references surfaced from the advisory trail, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260420-223939/`. All three candidates turned into clean substantive additions from this pass, so new advisory-mapped pages now capture `ip`'s SSRF-relevant `isPublic()` classification-bypass history including the later incomplete-fix advisory that still publicly affects `2.0.1`, `shell-quote`'s two command-injection records fixed in `1.6.1` and `1.7.3`, and `passport`'s pre-`0.6.0` session-fixation issue in login / logout session handling. The proxy synthesis step succeeded and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-20] advisory-review | github.com/gorilla/mux (Go), django (PyPI), openssl@3 (Homebrew)
Ran a public-information-only review pass across one clean Go router gap and two comparison candidates using OSV package queries, the public GitHub repository API, the public GitHub security-advisory listing, upstream README / repository checks, Homebrew formula metadata, PyPI metadata, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260420-183831/`. `github.com/gorilla/mux` was the only substantive update from this pass, so a new conservative baseline page now records that no direct package-scoped OSV or GitHub Security Advisory entry was confirmed here, preserves the evidence that the repository still showed public activity in 2024, and notes the absence of a confirmed `SECURITY.md` without turning that documentation gap into a vulnerability claim. `django` and `openssl@3` were re-screened as comparison candidates in the same window, but no page changes were forced because Django's public package history is too broad for a safe quick normalization pass and the Homebrew `openssl@3` formula still needs a wider upstream-versus-formula mapping pass rather than a misleading formula-scoped zero-advisory summary. The proxy synthesis step succeeded and was used only as a drafting aid; final edits were checked back against the saved public evidence before landing.

## [2026-04-20] advisory-review | postcss (npm), tmp (npm), xml2js (npm)
Ran a public-information-only review pass across three npm package gaps using OSV package queries, detailed OSV vulnerability records, GitHub Advisory Database records, public CVE aliases, upstream issue / PR / fix and release references surfaced from the advisory trail, npm registry metadata, the npm downloads API, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260420-1437/`. All three candidates turned into clean substantive additions from this pass, so new advisory-mapped pages now capture `postcss`'s two 2021 source-map ReDoS records plus the 2023 carriage-return parsing integrity issue fixed in `8.4.31`, `tmp`'s 2025 symlink-based temp-path boundary bypass fixed in `0.2.4`, and `xml2js`'s 2023 prototype-pollution issue fixed in `0.5.0`. The proxy synthesis step succeeded and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-20] advisory-review | js-yaml (npm), mathjs (npm), tar (npm)
Ran a public-information-only review pass across two npm pages that looked under-mapped against current OSV package results plus one comparison candidate using OSV package queries, detailed OSV vulnerability records, public CVE / NVD aliases, upstream changelog / HISTORY material, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260420-1037/`. `js-yaml` and `mathjs` both received substantive additive updates from this pass: `js-yaml` was promoted from a mostly audit-centered page to an advisory-mapped + audit-ingested page by adding the previously missing 2019 `safeLoad()` hang record, the 2019 `.load()` code-execution record, and the 2025 merge-key prototype-pollution fix while keeping the disputed alias-amplification audit note clearly separate from package-level advisories; `mathjs` was reconciled against the full public OSV package trail by adding the two 2017 arbitrary-code-execution records, the 2020 `math.config` prototype-pollution fix, and the second `15.2.0` expression-parser advisory that the existing page had only alluded to indirectly. `tar` was re-screened as a comparison candidate in the same window, but its page already matched the public OSV package count closely enough that no substantive edit was warranted from this pass. The proxy synthesis step succeeded and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

## [2026-04-20] advisory-review | serde (crates.io), express-session (npm), cors (npm)
Ran a public-information-only review pass across one lingering Rust seed page and two npm comparison candidates using an OSV API package query, RustSec advisory search, crates.io metadata, upstream README / repository security-policy checks, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260420-0637/`. `serde` was the only substantive update from this pass, so its page was upgraded from a thin seed stub into a conservative baseline page that explicitly records the absence of a direct package-scoped OSV / RustSec advisory in this review window, separates core `serde` from related `serde_*` crate advisories, and notes the lack of a confirmed repository-level `SECURITY.md` without turning that documentation gap into a vulnerability claim. `express-session` and `cors` were re-screened as comparison candidates, but the public package-scoped evidence reviewed here still did not justify stronger changes than the current baseline pages already carry. The proxy synthesis step succeeded and was used only as a drafting aid; final `serde` edits were checked back against the saved public evidence before landing.

## [2026-04-20] advisory-review | github.com/labstack/echo/v4 (Go), django (PyPI), github.com/gorilla/mux (Go)
Ran a public-information-only review pass across one clean Go web-framework gap and two comparison candidates using an OSV package query, detailed OSV vulnerability records, public GHSA / Go vulnerability aliases, upstream GitHub release notes, upstream `CHANGELOG.md`, and upstream `SECURITY.md`, with evidence saved under `raw/advisory-review-20260420-023819/echo-v4/`. `github.com/labstack/echo/v4` was the only substantive update from this pass, so a new advisory-mapped page now captures its two currently published package records: the Windows-only static-handler directory traversal issue fixed in `4.2.0` and the static-handler open redirect fixed in `4.9.0`, while also preserving current upstream support-policy context for maintained `v4` and `v5` lines. `django` and `github.com/gorilla/mux` were re-screened as comparison candidates in this maintenance window, but no page changes were forced because Django still needs a larger advisory-normalization pass and `gorilla/mux` still did not justify a confident package page from the compact public evidence reviewed here. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final Echo edits were checked back against the saved public evidence bundle before landing.

## [2026-04-19] advisory-review | cookie-parser (npm), cors (npm), helmet (npm)
Ran a public-information-only review pass across three high-usage npm middleware gaps using OSV package queries, authenticated GitHub Advisory Database API queries, npm registry metadata, the npm downloads API, upstream changelog / history material, and the required local Claude-compatible proxy at `http://127.0.0.1:8319`, with evidence saved under `raw/advisory-review-20260419-223816/`. All three candidates turned into conservative additive baseline pages rather than forced advisory-heavy summaries: `cookie-parser` remains a no-direct-advisory package in this pass but now captures evidence-backed dependency context around the `cookie@0.7.x` hardening line; `cors` now records that no package-scoped GHSA / OSV record was confirmed and explicitly frames its main risk boundary as downstream policy configuration; and `helmet` now records the same no-direct-advisory result while preserving the useful positive signal from its published `SECURITY.md` disclosure path. The proxy synthesis step succeeded and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-19] advisory-review | express-session (npm)
Ran a public-information-only review pass against one high-usage npm session-management gap using npm registry metadata, the npm downloads API, an OSV package query, upstream GitHub release metadata, upstream `HISTORY.md`, and spot-checks of public GHSA / CVE pages collected under `raw/advisory-review-20260419-143837/`. This pass added a conservative new baseline page for `express-session` rather than forcing an advisory-heavy summary: the page explicitly records that no clean package-scoped OSV / GHSA record was confirmed in this window, preserves the upstream `1.5.2` note about the `cookie-signature@1.0.4` timing-attack fix, and captures the package's important deployment-boundary cautions around session stores, cookie settings, and proxy trust without presenting misconfiguration as a package CVE. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-18] advisory-review | tough-cookie (npm), form-data (npm), cookie-signature (npm), mime (npm)
Ran a public-information-only review pass across four npm cookie / HTTP-client-adjacent packages using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream issue / commit / release references, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260418-223817/`. `tough-cookie`, `form-data`, and `cookie-signature` were all clean substantive additions from this pass, so new advisory-mapped pages now capture `tough-cookie`'s two older parser-ReDoS fixes plus the later `4.1.3` prototype-pollution repair, `form-data`'s 2025 predictable multipart-boundary fix across the maintained `2.x`, `3.x`, and `4.x` lines, and `cookie-signature`'s historical timing-attack fix in `1.0.4`. `mime` was screened as a comparison candidate in the same pass and its public advisory trail was gathered, but no page was forced this round because the maintenance window was spent on the three higher-leverage additions above. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved public advisory, commit, and release evidence before landing.

## [2026-04-18] advisory-review | jinja2 (PyPI), django (PyPI), github.com/gorilla/mux (Go)
Ran a public-information-only review pass across one high-value Python templating gap and two comparison candidates using OSV package queries, public GHSA / CVE aliases, PyPA advisory aliases, upstream changelog and release-note entries, PyPI metadata, and pypistats download data. `jinja2` was the only clean substantive update from this pass, so a new advisory-mapped page now captures its 10 normalized published vulnerabilities across the 2014 cache-directory issues, repeated sandbox-breakout fix trains, the 2020 `urlize` ReDoS, the 2024 `xmlattr` follow-on hardening sequence, and the 2025 `|attr` sandbox bypass fixed in `3.1.6`. `django` and `github.com/gorilla/mux` were screened as comparison candidates in the same pass, but no page changes were forced here because Django's package history is much larger than this maintenance window allowed and `gorilla/mux` again did not surface package-scoped OSV records. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `jinja2` edits were checked back against the public advisory and changelog evidence before landing.

## [2026-04-18] advisory-review | flask (PyPI), github.com/labstack/echo/v4 (Go), express-session (npm)
Ran a public-information-only review pass across one high-value Python framework gap and two comparison candidates using OSV package queries, public GHSA / CVE aliases, upstream changelog / release notes, PyPI metadata, and pypistats download data collected under `raw/advisory-review-20260418-143837/`. `flask` was the only clean substantive update from this pass, so a new advisory-mapped page now captures its five currently published package records across the older crafted-JSON / memory-usage denial-of-service issues, the 2023 cache-mediated session-cookie disclosure fixed in `2.2.5` / `2.3.2`, the 2025 fallback-key signing-order regression fixed in `3.1.1`, and the 2026 `Vary: Cookie` follow-on fix in `3.1.3`. `github.com/labstack/echo/v4` and `express-session` were screened as comparison candidates in the same pass, but no page changes were forced here because Echo still deserves a dedicated Go-specific normalization pass and `express-session` again did not surface package-scoped OSV records in this window. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `flask` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-18] advisory-review | multer (npm), flask (PyPI), github.com/labstack/echo/v4 (Go)
Ran a public-information-only review pass across one high-usage npm upload-middleware gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream GitHub security advisories, upstream release notes / changelog, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260418-1037/`. `multer` was the only clean substantive update from this pass, so a new advisory-mapped page now captures its seven currently published high-severity denial-of-service records across the 2025-2026 fix train from `2.0.0` through `2.1.1`, including the stream-leak, malformed-request, resource-exhaustion, cleanup, and uncontrolled-recursion fixes explicitly called out in upstream releases. `flask` and `github.com/labstack/echo/v4` were reviewed as comparison candidates in the same pass, but they still need dedicated advisory-by-advisory normalization passes rather than a rushed mixed-ecosystem summary. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `multer` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-18] advisory-review | http-proxy-middleware (npm), express-session (npm), cookie-parser (npm)
Ran a public-information-only review pass across one npm proxy-middleware gap and two comparison candidates using OSV package queries, GitHub Advisory Database records via OSV vulnerability entries, public CVE aliases, upstream changelog, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260418-0639/http-proxy-middleware/`. `http-proxy-middleware` was the only substantive update from this pass, so a new advisory-mapped page now captures its currently published package history across the 2024 path-filter denial-of-service issue and the 2025 `fixRequestBody` follow-on flaw chain fixed through `2.0.9` / `3.0.5`. `express-session` and `cookie-parser` were reviewed as comparison candidates in the same pass, but their package-scoped OSV queries returned empty at review time, so no speculative pages were forced. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-18] advisory-review | braces (npm), morgan (npm), express-session (npm)
Ran a public-information-only review pass across two npm package gaps and one comparison candidate using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream issue / PR / commit history, upstream changelog, Node.js Security WG advisory material, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260418-023754/`. `braces` and `morgan` were both clean substantive additions from this pass, so new advisory-mapped pages now capture `braces`'s published 2.x ReDoS issue plus the newer `3.0.3` imbalanced-input memory-exhaustion fix, and `morgan`'s 2019 format-compilation code-injection issue fixed in `1.9.1`. `express-session` was reviewed as a comparison candidate in the same pass, but its package-scoped OSV query returned empty in this window, so no speculative page was forced. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-17] advisory-review | cross-spawn (npm), yargs-parser (npm), braces (npm)
Ran a public-information-only review pass across two high-download npm gaps and one comparison candidate using OSV package queries, GitHub Advisory Database JSON records, public CVE aliases, upstream changelog / fix-commit metadata, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260418-033832/`. `cross-spawn` and `yargs-parser` were both clean substantive additions from this pass, so new advisory-mapped pages now capture `cross-spawn`'s 2024 ReDoS fix chain across `6.0.6` and `7.0.5`, plus `yargs-parser`'s 2020 prototype-pollution remediation across `5.0.1`, `13.1.2`, `15.0.1`, and `18.1.1`. `braces` was reviewed as a comparison candidate in the same pass, but its two-public-advisory history deserves a dedicated release-normalization pass rather than a rushed partial page in this maintenance window. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-17] advisory-review | marked (npm), express-session (npm), morgan (npm)
Ran a public-information-only review pass across one high-value npm markdown gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream `SECURITY.md`, upstream README / release notes, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260417-1838/marked/`. `marked` was the only substantive update from this pass, so a new advisory-mapped page now captures its long public vulnerability history across the older `sanitize: true` XSS / content-injection failures, repeated regex-complexity / ReDoS fixes, the `0.7.0` deprecation of built-in sanitization options, and the later `4.0.10` security release addressing the two 2022 ReDoS CVEs. `express-session` and `morgan` were screened as comparison candidates in the same pass, but no page changes were forced here because `express-session` did not surface package-scoped OSV records in this pass and `morgan` looked better suited to a separate narrower review rather than a rushed partial summary. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `marked` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-17] advisory-review | ejs (npm), marked (npm), express-session (npm)
Ran a public-information-only review pass across one high-value npm templating gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream `SECURITY.md`, upstream issue / commit history, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260417-1038/`. `ejs` was the only substantive update from this pass, so a new advisory-mapped page now captures the package's published security history across the older 2.x `renderFile()` XSS / DoS / RCE cluster, the later `CVE-2022-29078` server-side template injection issue fixed in `3.1.7`, and the 2024 prototype-pollution-protection gap fixed in `3.1.10`. `marked` and `express-session` were screened as comparison candidates in the same pass, but no page changes were forced here because this maintenance window was spent on the cleaner `ejs` evidence trail and the other candidates still deserve their own fuller normalization passes rather than rushed partial summaries. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `ejs` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-17] advisory-review | serialize-javascript (npm), ejs (npm), marked (npm)
Ran a public-information-only review pass across one clean npm gap and two comparison candidates using OSV package queries, GitHub Advisory Database / upstream GitHub security advisories, public CVE records, upstream GitHub release notes / README, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260417-0637/`. `serialize-javascript` was the only substantive update from this pass, so a new advisory-mapped page now captures the package's published vulnerability history across browser-context regex / URL XSS, incomplete-fix code-injection / RCE lineage, and the newer 2026 crafted-array-like-object CPU exhaustion denial of service fixed in `7.0.5`. `ejs` and `marked` were screened as comparison candidates in the same pass, but no page changes were forced here because this maintenance window was spent on the cleaner `serialize-javascript` evidence trail and the other candidates deserve their own broader normalization passes rather than rushed partial summaries. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `serialize-javascript` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-17] advisory-review | validator (npm), ejs (npm), express-session (npm)
Ran a public-information-only review pass across one high-value npm gap and two comparison candidates using OSV package queries, GitHub Advisory Database / upstream GitHub security advisories, public CVE records, the upstream `CHANGELOG.md` / `README.md`, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260417-0238/`. `validator` was the only substantive update from this pass, so a new advisory-mapped page now captures its published vulnerability history across the older `xss()` blacklist-filter bypass cluster, the historical `isURL()` ReDoS issue, the 2021 regex-complexity hardening release, and the newer 2025 `isURL` protocol-parsing plus `isLength` Unicode-length fixes. `ejs` and `express-session` were screened as comparison candidates in the same pass, but no page changes were forced here because this maintenance window was spent on the cleaner `validator` evidence trail and `express-session` did not surface package-scoped OSV records in this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `validator` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-17] advisory-review | github.com/gin-contrib/cors (Go), serde (crates.io), openssl (linux)
Ran a public-information-only review pass across one Go middleware gap and two comparison candidates using OSV package queries, the GitHub Advisory Database / public GHSA page, a public Go vuln record, public CVE aliases, upstream README / release / compare metadata, and upstream fix PR / commit references collected under `raw/advisory-review-20260417-1437/`. `github.com/gin-contrib/cors` was the only substantive update from this pass, so a new advisory-mapped page now captures the standalone middleware module's published wildcard-origin trust-boundary flaw `CVE-2019-25211` / `GHSA-869c-j7wc-8jqv` / `GO-2024-2955`, fixed in `1.6.0`, while also cleaning up cross-linking so the core Gin page no longer carries future-tense ambiguity about that middleware-specific record. `serde` was rechecked as a Rust comparison candidate and the package-scoped OSV query still returned empty in this pass, while Linux `openssl` was only lightly re-screened and still needs a broader upstream-plus-distro normalization pass rather than a rushed partial update. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `gin-contrib/cors` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-16] advisory-review | handlebars (npm)
Ran a public-information-only review pass against a high-usage npm gap using OSV package data, upstream GitHub security advisories, public CVE aliases, upstream release notes / `SECURITY.md`, GitHub release metadata, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260417-0345/`. Added a new `wiki/npm/handlebars.md` page and updated the npm plus master indexes so the KB now tracks Handlebars' long advisory history across XSS, prototype pollution, arbitrary code execution / JavaScript injection, ReDoS, and the coordinated eight-advisory `v4.7.9` hardening release. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved public evidence bundle before landing.

## [2026-04-16] advisory-review | serve-static (npm), cookie (npm), minimist (npm)
Ran a public-information-only review pass across one npm gap and two comparison candidates using GitHub Advisory Database records, OSV package queries, public CVE aliases, upstream `HISTORY.md`, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260416-1837/`. `serve-static` was the only substantive update from this pass, so a new advisory-mapped page now captures the package's two currently published records: the older open-redirect issue fixed in `1.7.2` and the redirect-page template-injection / XSS issue fixed in `1.16.0` and `2.1.0`. `cookie` and `minimist` were rechecked as comparison candidates in the same pass, but their package-scoped OSV results did not surface materially new published advisory content beyond what the KB already tracks, so no speculative refreshes were forced. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `serve-static` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-19] advisory-review | mime (npm), micromatch (npm), glob-parent (npm)
Ran a public-information-only review pass across three high-download npm package gaps using OSV package queries, GitHub Advisory Database / upstream GitHub security advisory material, public CVE records, upstream issue / PR / commit / release references surfaced through OSV, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260419-1038/`. All three candidates turned into clean substantive additions from this pass, so new advisory-mapped pages now capture `mime`'s historical MIME-lookup ReDoS fix split across the `1.x` and `2.x` lines, `micromatch`'s 2024 brace-expansion ReDoS fixed in `4.0.8` after earlier incomplete mitigation, and `glob-parent`'s two separate ReDoS records fixed in `5.1.2` and `6.0.1`. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final page content was checked back against the saved OSV, CVE, npm, and upstream-reference evidence before landing.

## [2026-04-19] advisory-review | send (npm), cookie-signature (npm), validator (npm)
## [2026-04-19] advisory-review | moment (npm), nanoid (npm), got (npm)
Ran a public-information-only review pass across three popular npm package gaps using OSV package queries, GitHub Advisory Database / GitHub security advisory material, public CVE records, upstream changelog and fix-reference material, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260419-0638/`. This pass added new advisory-mapped pages for `moment`, `nanoid`, and `got`: `moment` now captures two older ReDoS records plus the 2022 `moment.locale` traversal and RFC2822 regex fixes; `nanoid` now captures the 2021 object-coercion collision issue and the 2024 non-integer size predictability fix across both maintained lines; and `got` now captures the 2022 redirect-to-UNIX-socket boundary flaw fixed in `11.8.5` and `12.1.0`. The required local Claude-compatible proxy synthesis step was attempted via `http://127.0.0.1:8319`, but the proxy returned HTTP 429 rate-limit responses in this window, so final curation was completed manually from the saved public evidence bundle without relying on speculative drafting output.

## [2026-04-19] advisory-review | express-session (npm), cookie (npm), cookie-signature (npm)
Ran a public-information-only review pass across one npm baseline stub and two adjacent cookie/session pages using OSV package queries, GitHub Advisory Database / public GHSA pages, public CVE aliases, upstream `HISTORY.md`, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260419-183800/`. `cookie` and `express-session` received the only additive updates from this pass. `cookie` was refreshed to reflect current npm metadata and to make its public advisory mapping crisper: OSV package results in this pass showed a single package-level record, `GHSA-pxg6-pf52-xh8x` / `CVE-2024-47764`, covering cookie-attribute injection via out-of-bounds name/path/domain values fixed in `0.7.0`, with current latest metadata now well past the fix point. `express-session` remained a conservative baseline stub because no direct package-scoped OSV records were confirmed, but the page now captures evidence-backed dependency context from upstream `HISTORY.md`: `1.18.1` pulled in the fixed `cookie@0.7.x` line and `1.5.2` pulled in `cookie-signature@1.0.4` for the historical timing-attack fix. `cookie-signature` was rechecked as a comparison candidate and did not surface stronger new public package-level content than the KB already reflects, so no page changes were forced there. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

Ran a public-information-only review pass across one npm page needing status reconciliation and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream `HISTORY.md` / `README`, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260419-0238/`. `send` was the only substantive update from this pass, so its page was promoted from audit-ingested to advisory-mapped after reconciling the full public package advisory set: the historical `root`-containment traversal issue fixed in `0.8.4`, the `root` path-disclosure issue fixed in `0.11.1`, and the redirect-template injection / XSS issue fixed in `0.19.0`, with release-history notes clarifying that the modern `1.x` line carries forward the latest fix. `cookie-signature` and `validator` were rechecked as comparison candidates in the same pass, but their package-scoped OSV results did not surface stronger new published-advisory content than the KB already reflects, so no speculative refreshes were forced there. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `send` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-21] advisory-review | lodash (npm), cookie (npm), express (npm)
Ran a public-information-only review pass across one npm page with a real advisory gap and two status-reconciliation candidates using OSV package queries, GitHub Advisory Database advisory pages, public CVE aliases, upstream `History.md`, npm registry metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260421-023908/`. `lodash` was the only substantive content update from this pass: the page now includes the two older published prototype-pollution advisories that were missing from the KB (`CVE-2018-3721` / `GHSA-fvqr-27wr-82fm`, fixed in `4.17.5`, and `CVE-2018-16487` / `GHSA-4xc9-xhrj-v574`, fixed in `4.17.11`), which makes the package page line up with the fuller OSV package record. `cookie` and `express` were also rechecked in the same pass; their public advisory sets already matched the KB content, so the only worthwhile changes there were conservative status normalizations from audit-ingested to advisory-mapped plus corresponding index wording cleanup. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-21] advisory-review | markdown-it (npm), esbuild (npm), path-parse (npm)
Ran a public-information-only review pass across two fresh npm package gaps and one lower-priority comparison candidate using OSV package queries, GitHub Advisory Database / public GitHub security advisories, public CVE records, upstream changelog / release references, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260421-1437/`. `markdown-it` and `esbuild` were both substantive additions from this pass, so new advisory-mapped pages now capture `markdown-it`'s three currently published resource-exhaustion / ReDoS records across the pre-`3.0.0` line, the `12.3.2` newline-rule fix, and the `13.0.0` through `14.0.x` linkify regression fixed in `14.1.1`, plus `esbuild`'s currently published development-server CORS exposure fixed in `0.25.0`. `path-parse` was reviewed as a comparison candidate in the same pass, but its single older record already converges on the current latest `1.0.7`, so it was intentionally deferred rather than forced for completeness alone. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-21] advisory-review | webpack-dev-server (npm), yaml (npm), pac-resolver (npm)
Ran a public-information-only review pass across two npm package gaps and one comparison candidate using OSV package queries, GitHub Advisory Database / upstream GitHub security advisories, public CVE records, upstream release notes / changelog references, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260421-063928/`. `webpack-dev-server` and `yaml` were both substantive updates from this pass, so new advisory-mapped pages now capture `webpack-dev-server`'s published origin / cross-origin source-code exposure history across the 2018 WebSocket origin-validation bug and the 2025 `v5.2.1` hardening pair, plus `yaml`'s currently published parser-failure history across the `v2.2.2` degenerate-input exception fix and the `v2.8.3` deeply nested collection stack-overflow fix. `pac-resolver` was reviewed as a comparison candidate in the same pass, but its single public record was intentionally deferred rather than forced in this window. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final KB edits were checked back against the saved public evidence bundle before landing.

## [2026-04-23] advisory-review | vite (npm), pug (npm), vue (npm)
Ran a public-information-only review pass across one high-value npm dev-server gap and two comparison candidates using OSV package queries, GitHub Advisory Database / public GHSA pages, public CVE records, the upstream GitHub security policy page, upstream release notes, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260423-143936/`. `vite` was the only substantive update from this pass, so a new advisory-mapped page now captures its 19 currently published package records across repeated `server.fs` / file-boundary bypasses, dev-server cross-origin exposure, and a smaller XSS lineage in Vite-controlled HTML / injected script paths. `pug` and `vue` were screened as comparison candidates in the same pass, but their smaller public advisory sets did not justify rushing partial pages ahead of the much denser Vite gap in this window. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `vite` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-21] advisory-review | fastify (npm), cors (npm), helmet (npm)
Ran a public-information-only review pass across one high-value npm framework gap and two comparison candidates using OSV package queries, GitHub Advisory Database / upstream GitHub security advisories, public CVE records, upstream release notes, upstream `SECURITY.md`, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260421-183820/`. `fastify` was the only substantive update from this pass, so a new advisory-mapped page now captures its 10 currently published package records across recurring `Content-Type` parsing / validation-boundary flaws, proxy-trust issues in forwarded host / protocol handling, and JSON / stream-related denial-of-service history. `cors` and `helmet` were rechecked as comparison candidates in the same pass, but their package-scoped OSV queries remained empty here, so no filler edits were forced. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `fastify` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-24] advisory-review | Werkzeug (PyPI), Django (PyPI), express-session (npm)
Ran a public-information-only review pass across one high-value Python framework gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream `CHANGES.rst`, PyPI metadata, pypistats recent-download data, the NVD dispute record for `CVE-2022-29361`, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260424-023842/`. `Werkzeug` was the only substantive update from this pass, so a new advisory-mapped page now captures its public package history across debugger security flaws, multipart form-parser denial-of-service issues, cookie / redirect edge cases, and the newer multi-release Windows `safe_join()` bypass chain through `3.1.6`. `Django` was screened as a comparison gap but its 272-record OSV surface is too large for a safe partial page in this maintenance window, and `express-session` was spot-checked again as a comparison candidate without surfacing a cleaner direct package-level advisory set than its current baseline stub. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `Werkzeug` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-16] advisory-review | undici (npm), handlebars (npm), openssl (linux)
Ran a public-information-only review pass across two high-value npm gaps and one Linux comparison stub using OSV package queries, GitHub Advisory Database / public GHSA pages, public CVE aliases, upstream `SECURITY.md`, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260416-1438/`. `undici` was the only clean substantive update from this pass, so a new advisory-mapped page now captures its 22 currently published package records across redirect-boundary credential leakage, CRLF / request-smuggling style header bugs, SSRF / proxy boundary issues, integrity / randomness regressions, and the newer 2026 WebSocket / decompression / buffering denial-of-service cluster. `handlebars` and the Linux `openssl` stub were screened as comparison candidates in the same pass, but no page changes were forced because `handlebars` needs a broader release-history reconciliation than this window allowed and Linux `openssl` still needs a dedicated upstream-plus-distro normalization pass rather than a rushed partial update. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `undici` edits were checked back against the saved public evidence bundle before landing.


## [2026-04-16] advisory-review | undici (npm), node-fetch (npm), http-proxy-middleware (npm)
Ran a public-information-only review pass across one high-value npm gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream fix PR / commit references, GitHub release metadata, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260416-1038/`. `undici` was the only substantive update from this pass, so a new advisory-mapped page now captures its currently published package records across request-smuggling, header / parser boundary issues, and denial-of-service relevant behavior confirmed in public references. `node-fetch` and `http-proxy-middleware` were screened as comparison candidates in the same pass, but neither page was forced forward here because their existing coverage or evidence quality did not justify a speculative partial refresh in this window. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `undici` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-15] advisory-review | follow-redirects (npm), undici (npm), cookie (npm)
Ran a public-information-only review pass across one high-value npm gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream `SECURITY.md`, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260415-2237/`. `follow-redirects` was the only substantive update from this pass, so a new advisory-mapped page now captures its five currently published package records across repeated redirect-boundary credential leakage, URL parsing / redirect-target confusion, and the newer 2026 custom-auth-header leak fixed in `1.16.0`. `undici` and `cookie` were screened as comparison candidates in the same pass, but no page changes were forced here because `undici` needs a broader evidence pass than this window allowed and `cookie` did not surface a stronger evidence-backed update than its existing audit-ingested page. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `follow-redirects` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-15] advisory-review | node-fetch (npm), undici (npm), http-proxy-middleware (npm)
Ran a public-information-only review pass across one high-value npm gap and two comparison candidates using OSV package queries, GitHub Advisory Database records, public CVE aliases, upstream fix PR / commit references, GitHub release metadata, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260415-2237/`. `node-fetch` was the only substantive update from this pass, so a new advisory-mapped page now captures its three currently published package records across cross-origin redirect credential leakage, redirect-bypass of configured response-size limits, and the newer 3.x multipart-boundary ReDoS fixed in `3.2.10`. `undici` and `http-proxy-middleware` were screened as comparison candidates in the same pass, but both deserve a broader evidence pass than this window allowed and no speculative partial pages were forced here. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `node-fetch` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-15] advisory-review | express (npm), serde (crates.io), openssl (linux)
Ran a public-information-only review pass across one high-value npm refresh candidate and two comparison gaps using OSV package queries, GitHub Advisory Database JSON records, public CVE aliases, upstream `History.md`, and Express release notes collected under `raw/advisory-review-20260415-1038/`. `express` was the only substantive update from this pass: the page now reflects the broader published package-advisory set confirmed in public records, adding the older `CVE-2014-6393` charset/XSS issue plus the 2024 redirect and header-surface entries `CVE-2024-29041`, `CVE-2024-43796`, `CVE-2024-9266`, and `CVE-2024-10491`, while explicitly keeping the local audit findings separate as residual application-layer risk where appropriate. `serde` remained a no-forcing comparison candidate because the package-scoped OSV query stayed empty in this pass, and the Linux `openssl` baseline stub was intentionally left untouched because it needs a broader, dedicated evidence pass rather than a rushed partial update. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

## [2026-04-14] advisory-review | ws (npm), serde (crates.io), openssl@3 (Homebrew)
Ran a public-information-only review pass across one high-blast-radius npm gap and two comparison candidates using an OSV package query, GitHub Advisory Database / repository security-advisory material, public CVE aliases, upstream release notes, the repository security-policy page, npm registry metadata, and npm download data collected under `raw/advisory-review-20260414-0637/`. `ws` was the only substantive update from this pass, so a new advisory-mapped page now captures its published vulnerability history across the 2016 remote memory disclosure, large-payload DoS, 2017 `Sec-WebSocket-Extensions` parser crash, 2021 `Sec-Websocket-Protocol` ReDoS, and the 2024 excessive-header-count crash fixed across multiple maintained major lines. `serde` was rechecked as a Rust comparison candidate and still showed no package-scoped OSV records in this pass, while `openssl@3` was only lightly checked through public formula metadata and did not justify page changes. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `ws` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-14] advisory-review | body-parser (npm), koa-router (npm), serde (crates.io)
Ran a public-information-only review pass across one npm audit-ingested page and two comparison candidates using OSV package queries, GitHub Advisory Database / repository advisory listings, public CVE aliases, upstream `HISTORY.md`, and GitHub release metadata collected under `raw/advisory-review-20260414-0237/`. `body-parser` was the only substantive update from this pass: the page now reflects both published package advisories confirmed in public records, including the 1.x `CVE-2024-45590` / `GHSA-qwcr-r2fm-qrc7` fix in `1.20.3` and the 2.x `CVE-2025-13466` / `GHSA-wqch-xfxh-vrr4` fix in `2.2.1`, while preserving the earlier source-audit findings about parser-boundary behavior. `koa-router` and `serde` were reviewed again as comparison candidates, but their package-scoped OSV queries and repository advisory listings remained empty in this pass, so no speculative changes were forced. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

## [2026-04-13] advisory-review | golang.org/x/crypto (Go), github.com/gorilla/mux (Go), serde (crates.io)
Ran a public-information-only review pass across one high-value Go module gap and two comparison candidates using OSV package queries, Go vuln records, GitHub Advisory Database entries, public CVE aliases, OpenSSH 9.6 release notes, golang-announce posts, and upstream fix references collected under the latest `raw/advisory-review-*/xcrypto/` evidence bundle. `golang.org/x/crypto` was the only substantive update from this pass, so a new advisory-mapped page now captures its broad public vulnerability history across repeated `ssh` boundary flaws plus `cryptobyte`, `openpgp/clearsign`, `acme/autocert`, and low-level cipher issues. `github.com/gorilla/mux` and `serde` were reviewed as comparison candidates but still showed no clean package-level OSV history worth forcing into the KB in this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `golang.org/x/crypto` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-16] advisory-review | follow-redirects (npm), undici (npm)
Ran a public-information-only review pass against two high-volume npm HTTP-client dependencies using OSV package queries, GitHub Advisory Database / public repository advisories, upstream `SECURITY.md`, current upstream source inspection, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260416-0238/`. `follow-redirects` was the cleanest substantive gap from this pass, so a new advisory-mapped page now captures its five published records across redirect-boundary sensitive-header leakage and malformed-URL redirect-target handling, including the latest `1.16.0` custom-auth-header fix. `undici` was reviewed as a comparison candidate in the same pass, but its much larger 22-advisory surface spans too many distinct bug classes to normalize safely in this maintenance window, so it was intentionally deferred for a dedicated pass rather than summarized hastily. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `follow-redirects` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-13] advisory-review | tar (npm), ws (npm), handlebars (npm)
Ran a public-information-only review pass across three npm candidates using OSV package queries, GitHub advisory records, CVE aliases, npm registry metadata, npm download data, and public fix references collected under `raw/advisory-review-20260413-1837/`. `tar` was the only clean substantive addition from this pass, so a new advisory-mapped page now captures its long-running extraction-safety history across legacy overwrite bugs, the dense 2021 symlink / cache-poisoning chain, the 2024 parser DoS, and the newer 2025-2026 `7.5.x` hardlink / symlink traversal follow-ons. `ws` and `handlebars` were reviewed as comparison candidates but not forced into the KB in this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `tar` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-07] bootstrap | repository structure
Initialized canonical wiki scaffolding: master index, append-only log, page template, CONTRIBUTING.md, and SECURITY.md.

## [2026-04-07] seed-pages | lodash, minimist, semver (npm)
Added first three high-value npm package pages with advisory-mapped baseline coverage and open questions. Updated wiki/index.md and wiki/npm/index.md.

## [2026-04-07] research-pass | npm seed package evidence
Expanded evidence and posture notes for lodash, minimist, and semver using advisory/search-backed sources. Added stronger context about recurring bug classes, exploitability, and follow-up audit questions.

## [2026-04-08] ingest | axios (npm)
Added axios package page documenting the 2026 compromised publish / supply-chain incident, safe downgrade targets, and open research questions around incident response and downstream exposure.

## [2026-04-11] advisory-review | requests (PyPI), google.golang.org/grpc (Go)
Ran a public-information-only review pass against two baseline stubs and upgraded both pages to advisory-mapped status. Curated Requests credential / redirect / TLS verification advisories and grpc-go transport / metadata / authorization findings using OSV, GitHub Security Advisories, upstream release notes, and CVE references. Updated `wiki/python/index.md`, `wiki/go/index.md`, and the master index descriptors to reflect the deeper coverage.

## [2026-04-11] advisory-review | Newtonsoft.Json (NuGet), jsonwebtoken (npm), koa-router (npm), serde (crates.io)
Ran a public-information-only review pass across one .NET page, one npm page refresh, and two gap candidates. Upgraded `Newtonsoft.Json` from baseline stub to advisory-mapped status using OSV, GitHub Security Advisories, the CVE record, and upstream issue / PR / fix / release history for the deeply nested JSON denial-of-service issue fixed in 13.0.1, including release-note-backed hardening context around `MaxDepth`. Refreshed `jsonwebtoken` so the page now reflects the accepted GHSA/CVE set around verification semantics, explicitly marks CVE-2022-23529 as rejected, and keeps the filed `clockTolerance` issue separate from published advisories. Also checked `koa-router` and `serde` for package-specific advisory history in OSV and GitHub advisory listings, but found no clean new package-level disclosures worth forcing into the KB in that pass.

## [2026-04-12] advisory-review | express (npm), koa-router (npm), serde (crates.io)
Ran a public-information-only review pass against three candidates using OSV package queries and GitHub repository advisory listings saved under `raw/advisory-review-2026-04-12/`. Public evidence showed multiple package advisories for `express`, while `koa-router` and `serde` returned no package-scoped OSV matches and no repository security advisories in this pass. The requested local Claude CLI synthesis step could not complete because the installed CLI returned a 401 authentication error, so no evidence-backed package-page edits were forced from this pass. Fixed unresolved merge conflict markers that had landed in `wiki/index.md` and `wiki/log.md` on `main`, and preserved the gathered evidence files for the next review.

## [2026-04-12] cleanup | index, npm index, template
Resolved leftover merge-conflict markers in `wiki/index.md` and `wiki/log.md`, preserving the useful details from both sides of the prior advisory-review history. Promoted `axios` into the npm seed-page section so ecosystem navigation matches its existing advisory-mapped page state, and aligned `wiki/TEMPLATE.md` status values with the public schema for cleaner future page generation.

## [2026-04-12] advisory-review | semver (npm), minimist (npm), express (npm)
Ran a public-information-only refresh pass using OSV package queries, GitHub repository advisory listings, CVE references, and upstream release/changelog material saved under `raw/advisory-review-2026-04-12-0946/`. Refreshed `semver` and `minimist` to capture an additional published advisory each (`CVE-2015-8855` / `GHSA-x6fg-f45m-jf5q` for semver and `CVE-2021-44906` / `GHSA-xvch-5gv4-984h` for minimist), and tightened posture notes so both pages reflect the recurring bug classes visible in public records. Also corrected the master index package count after prior page additions. Re-ran the requested local Claude CLI drafting step explicitly with `claude --permission-mode bypassPermissions --print --model opus`, but the installed local CLI returned a 401 authentication error again, so no Claude-authored synthesis was relied on for these edits.

## [2026-04-12] advisory-review | basic-ftp (npm), next-intl (npm), mathjs (npm)
Ran a public-information-only review pass across three fresh npm-package gaps using GitHub Advisory Database records, OSV entries, upstream changelogs / history, fix PRs or commits, GitHub release metadata, and npm registry context collected under `raw/advisory-review-2026-04-12-1346/`. Added new package pages for `basic-ftp`, `next-intl`, and `mathjs`, each documenting the currently published advisory set confirmed in this pass and explicitly noting where no public CVE identifier was present in the gathered evidence. The required local Claude CLI synthesis step was attempted with `claude --permission-mode bypassPermissions --print --model opus`, but the installed CLI returned a 401 authentication failure during non-interactive execution, so final curation was completed manually from the saved public evidence bundle.

## [2026-04-12] advisory-review | urllib3 (PyPI)
Ran a public-information-only review pass against a high-value Python gap using OSV package data, GitHub Advisory Database / GHSA records, CVE aliases surfaced through OSV, upstream `CHANGES.rst`, GitHub release metadata, and the repository security policy collected under `raw/advisory-review-2026-04-12-1246/`. Added a new `wiki/python/urllib3.md` page and updated the Python and master indexes so the KB now tracks urllib3's long-running advisory history across redirect credential leakage, TLS / certificate-validation edge cases, parser / CRLF bugs, and newer decompression-bomb issues in the streaming API. The requested local Claude CLI drafting step was attempted explicitly with `claude --permission-mode bypassPermissions --print --model opus`, but the installed CLI still returned a 401 authentication failure, so the final page was curated manually from the saved public evidence bundle before landing.

## [2026-04-12] advisory-review | System.Text.Json (NuGet), github.com/gin-gonic/gin (Go)
Ran a public-information-only review pass across one .NET gap and one Go gap using OSV package queries, GitHub Advisory Database / GHSA entries, public CVE records, Microsoft / upstream announcement and release material, and upstream issue / PR history collected under `raw/advisory-review-2026-04-12-1646/`. Added new pages for `System.Text.Json` and `github.com/gin-gonic/gin`, upgraded both ecosystem indexes plus the master index, and kept the Gin page conservative by excluding `CVE-2019-25211` from the core vulnerability table because the public references in this pass pointed primarily to `gin-contrib/cors` rather than cleanly to core Gin itself. The required local Claude CLI synthesis step was attempted explicitly with `claude --permission-mode bypassPermissions --print --model opus`, but the installed CLI again returned a 401 authentication failure, so final curation was completed manually from the saved public evidence bundle.

## [2026-04-12] advisory-review | debug (npm)
Ran a public-information-only review pass against a fresh npm gap using OSV package data, GitHub Advisory Database / GHSA entries, the public CVE record, the maintainer tracking issue for the 2025 compromise, the repository security-policy page, and npm registry / download metadata collected under `raw/advisory-review-2026-04-12-1837/`. Added a new `wiki/npm/debug.md` page and updated the npm plus master indexes so the KB now captures both `debug`'s older regex-complexity / ReDoS advisory lineage and the much newer `4.4.2` supply-chain compromise. Re-ran the requested local Claude CLI drafting step explicitly with `claude --permission-mode bypassPermissions --print --model opus`, but the installed local CLI again returned a 401 authentication failure, so final curation was completed manually from the saved public evidence bundle.

## [2026-04-12] advisory-review | basic-ftp (npm), path-to-regexp (npm)
Ran a public-information-only refresh pass against two npm pages with visible advisory gaps using OSV package records, GitHub Advisory Database / GHSA entries, public CVE aliases, and upstream GitHub release metadata collected under `raw/advisory-review-2026-04-12-1847/`. Refreshed `basic-ftp` so the page now reflects the full public 5.2.x vulnerability chain: the `downloadToDir` path-traversal fix in 5.2.0, the follow-on CRLF command-injection issue fixed in 5.2.1, and the incomplete-fix bypass remediated in 5.2.2. Refreshed `path-to-regexp` so the page now tracks the published advisory set across both the legacy 0.1.x line and modern 8.x line, including the 2026 wildcard and optional-group DoS fixes called out in the upstream 8.4.0 release notes, while keeping public Issue #433 separate from the GHSA/CVE table. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

## [2026-04-13] advisory-review | qs (npm)
Ran a public-information-only review pass against a high-value npm parser gap using OSV package queries, GitHub Advisory Database / GHSA records, CVE aliases, the upstream `qs` changelog, npm registry metadata, and the npm downloads API collected under `raw/advisory-review-20260413-0238/`. Added a new `wiki/npm/qs.md` page and updated the npm plus master indexes so the KB now captures `qs`'s long-running advisory history across early parser-complexity / memory-exhaustion bugs, prototype-pollution bypasses, and newer `arrayLimit` enforcement bypasses in bracket and comma parsing modes. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `qs` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-12] dispute-update | js-yaml alias expansion finding
Updated the js-yaml KB language after upstream maintainer feedback on issue #742. The alias-expansion / Billion Laughs claim is now recorded as a disputed finding / operational safety concern rather than a confirmed package vulnerability, while preserving the audit evidence and downstream-risk context.

## [2026-04-24] advisory-review | vue (npm), xmldom (npm)
Ran a public-information-only review pass against two npm gaps using OSV package queries, GitHub Advisory Database / public GitHub security advisories, public CVE records, maintainer fix commits and release tags, npm registry metadata, and npm downloads API gathered under `raw/advisory-review-20260424-1437/`. Added new package pages for `vue` and `xmldom`, keeping the Vue entry conservative around a single published Vue 2 `parseHTML` ReDoS record while documenting the `xmldom` package family's broader 2021-2026 advisory trail across malformed-input interpretation, multi-root parsing integrity failures, XML serialization injection, and deep-recursion DoS. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

## [2026-04-25] advisory-review | uuid (npm), express-session (npm), cookie-parser (npm)
Ran a public-information-only review pass across one high-value npm package gap and two previously baselined Express middleware comparison candidates using OSV package queries, OSV vulnerability records, GitHub Advisory Database pages, upstream security-policy / changelog / fix-commit material, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260425-0237/`. `uuid` was the only substantive update from this pass, so a new advisory-mapped page now captures the newly published `GHSA-w5hq-g745-h8pq` bounds-checking flaw in caller-supplied buffer writes for `v3()`, `v5()`, and `v6()`, fixed in `14.0.0`. `express-session` and `cookie-parser` were re-screened as comparison candidates, but this pass still did not surface a clean direct package-level GHSA / OSV record strong enough to justify replacing their conservative baseline-stub treatment. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `uuid` edits were checked back against the saved public evidence bundle before landing.

## [2026-04-24] advisory-review | postcss (npm), basic-ftp (npm), debug (npm)
Ran a public-information-only review pass across two npm page refresh candidates and one comparison candidate using OSV package queries, GitHub Advisory Database / public GHSA pages, public CVE records where available, upstream release references, npm registry metadata, npm downloads API, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260425-0337/`. `postcss` and `basic-ftp` were both substantive refreshes in this pass: `postcss` now includes the newly published `CVE-2026-41305` / `GHSA-qx2v-qp2m-jg93` XSS issue in CSS stringify output fixed in `8.5.10`, and `basic-ftp` now includes the newer `GHSA-rp42-5vxx-qpwr` unbounded-memory / `Client.list()` denial-of-service issue fixed in `5.3.0`. `debug` was reviewed as a comparison candidate, but no page change was forced because the extra OSV identifiers surfaced in this pass were aliases of the already-documented 2025 compromised-release incident rather than a distinct new package vulnerability. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against the saved public evidence bundle before landing.

- 2026-04-30: Added CVE-2026-31431 (Copy Fail) reference: https://xint.io/blog/copy-fail-linux-distributions

## [2026-05-05] advisory-review | cryptography (PyPI), Django (PyPI), Pillow (PyPI)
Ran a public-information-only review pass across three high-blast-radius Python package gaps using OSV package queries, OSV vulnerability records, GitHub Advisory Database / GHSA links, public CVE/NVD records, upstream `CHANGELOG.rst`, PyPI metadata, PyPIStats download data, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260505-0238/`. `cryptography` was the only substantive update from this pass, so a new advisory-mapped page now captures its public vulnerability history across cryptographic primitive validation, X.509 / PKCS parsing and verification, native-backed Python buffer handling, and bundled OpenSSL wheel exposure through the 2026 46.0.5-46.0.7 fix train. `Django` and `Pillow` were screened as comparison candidates, but their very large advisory histories need dedicated normalization passes rather than rushed partial pages. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `cryptography` edits were checked back against the saved public evidence bundle before landing.

## [2026-05-05] advisory-review | aiohttp (PyPI), Pillow (PyPI), Django (PyPI)
Ran a public-information-only review pass across three Python gaps using OSV package queries, OSV vulnerability records, GitHub Advisory Database / GHSA links, public CVE/NVD aliases, upstream changelog, PyPI metadata, PyPIStats download data, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260505-0638/`. `aiohttp` was the substantive update from this pass, so a new advisory-mapped page now captures 33 unique public package advisories across HTTP parser / request-smuggling ambiguity, CRLF / response-splitting injection, redirect credential leakage, static-file path exposure, multipart limits, and denial-of-service/resource-exhaustion issues through the `3.13.4` security fix train. `Pillow` and `Django` were screened as comparison candidates, but their 114- and 272-record OSV histories respectively need dedicated normalization passes rather than rushed partial pages. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `aiohttp` edits were checked back against the saved public evidence bundle before landing.

## [2026-05-06] advisory review | litellm (PyPI)
Source: OSV.dev PyPI `litellm` query/details, GitHub Advisory Database records, NVD CVE records, LiteLLM advisory/release links, and PyPI incident report. Expanded `wiki/python/litellm.md` from incident-focused coverage to distinct advisory mapping for public proxy/API vulnerabilities while preserving malicious-release identifiers as one supply-chain incident. Updated Python and master index descriptors.

## [2026-05-07] advisory-review | starlette (PyPI), fastapi (PyPI), sqlalchemy (PyPI)
Ran a public-information-only review pass across three Python ASGI / framework-adjacent candidates using OSV package queries, GitHub Advisory Database records, public CVE / PYSEC aliases, upstream fix commits, Starlette release notes, PyPI metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260507-0206/`. `starlette` was the substantive update from this pass, so a new advisory-mapped page now captures five unique public records across multipart-parser denial of service, `StaticFiles` path containment, multipart text-field memory exhaustion, large-upload event-loop blocking, and `FileResponse` Range-header CPU exhaustion through the `0.49.1` fix. `fastapi` and `sqlalchemy` were screened as comparison candidates and remain good future review targets, but no rushed partial page was forced in this maintenance window. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final Starlette edits were checked back against the saved public OSV / GHSA / upstream evidence bundle before landing.

## [2026-05-07] advisory-review | fastapi (PyPI), sqlalchemy (PyPI), pillow (PyPI)
Ran a public-information-only review pass across three Python candidates using OSV package queries, OSV vulnerability details, GitHub Advisory Database records, public CVE / PYSEC aliases, upstream FastAPI release notes / fix commits / security policy, PyPI metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260507-0605/`. `fastapi` was the substantive low-risk update from this pass, so a new advisory-mapped page now captures two unique public vulnerability classes: the 2021 JSON `Content-Type` CSRF boundary fixed in 0.65.2 and the 2024 dependency-mediated `python-multipart` ReDoS exposure addressed by FastAPI 0.109.1's minimum-version bump. `sqlalchemy` was screened as a manageable future candidate and `pillow` was confirmed to need a dedicated normalization pass because of its large public advisory history. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final FastAPI edits were checked back against saved public OSV / GHSA / upstream evidence before landing.
## [2026-05-08] advisory-review | sqlalchemy (PyPI), django (PyPI), pydantic (PyPI)
Ran a public-information-only review pass across three Python candidates using OSV package queries, OSV vulnerability details, GitHub Advisory Database records, public CVE / PYSEC aliases, upstream SQLAlchemy issue and commit references, distro advisory references, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260508-0205/`. `sqlalchemy` was the substantive low-risk update from this pass, so a new advisory-mapped page now captures three unique public SQL-injection vulnerability classes: the 2012 SQL construction issue and the 2019 `order_by()` / `group_by()` textual-coercion issues, including the fixed-version discrepancy between active GHSA-derived records and older PYSEC duplicates. `django` and `pydantic` were screened as comparison candidates; Django remains too broad for a rushed partial normalization pass, while Pydantic's small record set remains a good future compact target. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final SQLAlchemy edits were checked back against saved public evidence before landing.

## [2026-05-09] advisory-review | pydantic (PyPI), celery (PyPI), django (PyPI)
Ran a public-information-only review pass across three Python candidates using OSV package queries, OSV vulnerability details, GitHub Advisory Database records, public CVE / PYSEC aliases, upstream Pydantic release and commit / PR references, PyPI metadata, GitHub repository metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260509-0205/`. `pydantic` was the substantive low-risk update from this pass, so a new advisory-mapped page now captures two unique public DoS vulnerability classes: the 2021 `infinity` date / datetime infinite-loop issue and the 2024 email-validation ReDoS fixed in 1.10.13 and 2.4.0. `celery` was screened as an already-present comparison candidate and `django` was sampled again but remains too broad for a rushed partial normalization pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final Pydantic edits were checked back against saved public OSV / GHSA / upstream evidence before landing.

## [2026-05-09] advisory-review | golang.org/x/net (Go), twisted (PyPI), tomcat-embed-core (Maven)
Ran a public-information-only review pass across three package candidates using OSV package queries, OSV vulnerability details, GitHub Advisory Database aliases/pages, Go issue tracker records, Go announce posts, and Go change-list references collected under `raw/advisory-review-20260509-1005/`. `golang.org/x/net` was the substantive low-risk update from this pass, so a new advisory-mapped page now captures representative public vulnerability classes across HTTP/2 denial-of-service/resource exhaustion, `h2c` request-smuggling behavior, HTML parser / renderer XSS or non-linear parsing issues, and 2025 proxy-boundary handling. `twisted` and `org.apache.tomcat.embed:tomcat-embed-core` were screened as rich future candidates, but their large public advisory histories need dedicated normalization passes rather than rushed partial pages. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final `golang.org/x/net` edits were checked back against saved public OSV / GHSA / Go upstream evidence before landing.

## [2026-05-09] advisory-review | h2 (crates.io), koa (npm), go-chi/chi v5 (Go)
Ran a public-information-only review pass across three web-stack candidates using OSV package queries, OSV vulnerability details, GitHub Advisory Database records, RustSec / Go vuln aliases surfaced through OSV, upstream changelog / history / security-policy material, registry metadata, npm downloads data, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260509-1405/`. `h2` and `koa` were substantive low-risk additions: the new `h2` page captures three public HTTP/2 resource-exhaustion / denial-of-service advisories through the `0.3.26` / `0.4.4` fix points, while the new `koa` page captures five public advisories across ReDoS, `ctx.redirect()` XSS, redirect-back open redirects, and host-header injection through `2.16.2` / `3.1.2`. `github.com/go-chi/chi/v5` was re-screened as a comparison candidate, but its current page already captured the relevant `RedirectSlashes` open-redirect chain, so no duplicate page update was forced. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against saved public evidence before landing.

## [2026-05-10] advisory review | commons-fileupload:commons-fileupload (Maven)
Source: OSV.dev / GitHub Advisory Database records for commons-fileupload:commons-fileupload, Maven Central metadata, Apache Commons security policy, and upstream changes.xml. Added advisory-mapped Maven page covering CVE-2013-0248, CVE-2013-2186, CVE-2014-0050, CVE-2016-3092, CVE-2016-1000031, CVE-2023-24998, and CVE-2025-48976; updated Maven and master indexes. Proxy synthesis succeeded and was used only as a drafting aid.

## [2026-05-10] advisory-review | github.com/golang-jwt/jwt (Go)
Ran a public-information-only review pass against a compact Go authentication-library gap using OSV package queries for `github.com/golang-jwt/jwt`, `/v4`, and `/v5`, normalized OSV vulnerability records, GitHub Advisory Database entries, Go vulnerability database records, public CVE records, upstream release notes / version history, upstream SECURITY.md, fix commits, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260510-2206/`. Added a new advisory-mapped Go page documenting the v4-only `CVE-2024-51744` / `GHSA-29wx-vh33-7x7r` `ParseWithClaims` error-handling boundary and the cross-version `CVE-2025-30204` / `GHSA-mh63-6h87-95cp` `ParseUnverified` allocation-amplification DoS fixed in `/v4` 4.5.2 and `/v5` 5.2.2, while keeping the older `CVE-2020-26160` jwt-go lineage fix as context rather than a separate current advisory row. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked back against saved public evidence.

## [2026-05-11] advisory-review | commons-io:commons-io (Maven)
Ran a public-information-only review pass against a compact Java utility-library gap using OSV.dev / GitHub Advisory Database records, public CVE / NVD aliases, the Apache Commons IO security page, Maven Central metadata, upstream release notes, and the configured local Claude-compatible proxy. Added a new advisory-mapped Maven page for `commons-io:commons-io` covering `CVE-2021-29425` / `GHSA-gwrp-pvrq-jmwv` path-normalization traversal risk fixed in 2.7 and `CVE-2024-47554` / `GHSA-78wr-2p64-hpwj` `XmlStreamReader` CPU resource-consumption DoS fixed in 2.14.0, including the public severity discrepancy between GHSA/OSV and the Apache component advisory. Updated Maven and master indexes. Proxy synthesis succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against public evidence.
## [2026-05-11] advisory-review | org.springframework:spring-webmvc (Maven)
Ran a public-information-only review pass against a high-impact Java web-framework gap using OSV.dev package and vulnerability records, GitHub Advisory Database records, public CVE/NVD references, Spring security advisories, upstream issues / commits surfaced from OSV, Maven Central metadata, upstream SECURITY.md, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260511-0605/`. Added a new advisory-mapped Maven page for `org.springframework:spring-webmvc` covering 18 public advisories across Spring4Shell data-binding RCE, recurring static-resource/path traversal, XXE converter hardening, CSRF / request-matcher boundaries, reflected file download / XSS, cache poisoning, Server-Sent Events stream integrity, and denial-of-service issues through the 6.2.18 / 7.0.7 fix train. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against public evidence.


## [2026-05-12] advisory-review | org.springframework:spring-webflux (Maven)
Ran a public-information-only review pass against a compact Spring reactive web-framework gap using OSV.dev package and vulnerability records, GitHub Advisory Database records surfaced through OSV, public CVE/NVD references, Spring security advisories, upstream SECURITY.md, Maven Central metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260512-0205/`. Added a new advisory-mapped Maven page for `org.springframework:spring-webflux` covering 10 public advisories across Spring4Shell data-binding RCE, functional static-resource path traversal, CORS preflight CSRF, reflected file download, script-template file disclosure, SSE stream integrity, multipart temporary-file DoS, static-resource resolution DoS, and cache poisoning through the 6.2.18 / 7.0.7 fix train. `io.netty:netty-codec-http` and `org.eclipse.jetty:jetty-server` were screened as future candidates but not updated in this pass. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final Spring WebFlux edits were checked against public evidence.

## [2026-05-12] advisory-review | org.apache.kafka:kafka-clients (Maven)
Ran a public-information-only review pass across Maven `org.apache.kafka:kafka-clients`, Maven `org.apache.httpcomponents:httpclient`, Maven `org.apache.commons:commons-lang3`, and PyPI `httpx` using OSV.dev package queries, OSV vulnerability records, GitHub Advisory Database aliases surfaced through OSV, public CVE / NVD references, Apache Kafka's public CVE list, Maven Central metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260512-2305/`. `org.apache.kafka:kafka-clients` was the substantive low-risk update from this pass, so a new advisory-mapped Maven page now captures seven public package-scoped advisories across OAUTHBEARER / JWT validation, producer buffer-pool message corruption and misrouting, OAuth bearer endpoint file/SSRF exposure, ConfigProvider filesystem read, sensitive DEBUG logging, timing/session behavior, and authenticated-client impersonation through the 3.9.2 / 4.0.2 / 4.1.2 / 4.2.0 fix train. The proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public OSV / GHSA / Apache / Maven Central evidence.

## [2026-05-13] advisory-review | httpx (PyPI), org.apache.commons:commons-lang3 (Maven)
Ran a public-information-only review pass across PyPI `httpx` and Maven `org.apache.commons:commons-lang3` using OSV.dev package queries, OSV vulnerability details, GitHub Advisory Database records, public CVE records, upstream issue / PR / release and changelog references, Apache Commons public security / mailing-list advisory material, registry metadata, PyPIStats download data, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260513-1104/`. Added two low-risk advisory-mapped pages: `httpx` now captures the single unique public URL input-validation advisory (`CVE-2021-41945` / `GHSA-h8pj-cxx2-jfg2`) while explicitly noting the duplicate PYSEC fixed-version discrepancy, and `commons-lang3` now captures the `ClassUtils.getClass(...)` uncontrolled-recursion / `StackOverflowError` DoS (`CVE-2025-48924`) fixed in 3.18.0 with legacy 2.x coordinate context. The required proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public evidence.
## [2026-05-13] advisory-review | python-multipart (PyPI)
Ran a public-information-only review pass against PyPI `python-multipart`, a compact high-blast-radius multipart/form parser used by FastAPI / Starlette-style upload paths. Sources gathered under `raw/advisory-review-20260513-1006/` include OSV package and vulnerability records, GitHub Advisory Database / upstream GHSA records, public CVE aliases, upstream release / changelog material, PyPI metadata, PyPIStats recent downloads, and upstream `SECURITY.md`. Added a new advisory-mapped Python page covering five public advisories: the 2024 `Content-Type` header ReDoS fixed in `0.0.7`, the 2024 deformed-boundary DoS fixed in `0.0.18`, the 2026 non-default upload filename arbitrary-write/path traversal fixed in `0.0.22`, the 2026 large preamble / epilogue DoS fixed in `0.0.26`, and the 2026 unbounded part-header DoS fixed in `0.0.27`. Updated Python and master indexes. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public evidence.

## [2026-05-13] advisory-review | gunicorn, uvicorn, Flask-CORS (PyPI)
Ran a public-information-only review pass across three compact Python web-stack gaps using OSV.dev package queries, GitHub Advisory Database records surfaced through OSV, public CVE / NVD aliases, upstream issue / PR / commit references where present, upstream release or changelog material, PyPI metadata, PyPIStats downloads, repository security-policy pages, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260513-1806/`. Added three low-risk advisory-mapped pages: `gunicorn` now captures CRLF response/header injection plus the 2024 HTTP request/response-smuggling fix boundary at `22.0.0`; `uvicorn` now normalizes its two 2020 output-boundary records (log injection and HTTP response splitting) fixed in `0.11.7`; and `flask-cors` now captures six unique records across directory traversal, private-network header defaults, debug-log injection, and 2024 CORS matching / case-sensitivity fixes through `6.0.0`. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public evidence.
## [2026-05-14] advisory-review | h11 (PyPI), httpclient5 (Maven)
Ran a public-information-only review pass across PyPI `h11`, Maven `org.apache.httpcomponents.client5:httpclient5`, and Go `github.com/gorilla/mux` using OSV.dev package queries and vulnerability records, GitHub Advisory Database / GHSA records surfaced through OSV, public CVE / NVD records, upstream fix commit / PR references, Apache public advisory threads, oss-security posting for CVE-2026-40542, PyPI / PyPIStats metadata, Maven Central metadata, and the configured local Claude-compatible proxy collected under `raw/advisory-review-20260514-1006/`. Added two low-risk advisory-mapped pages: `h11` now captures the malformed chunked-transfer parsing / request-smuggling boundary fixed in `0.16.0`, and `httpclient5` now tracks the 5.x coordinate's two public advisories for TLS domain-check regression (`CVE-2025-27820`) and SCRAM-SHA-256 mutual-authentication verification (`CVE-2026-40542`) through `5.6.1`. `github.com/gorilla/mux` was screened as a comparison candidate, but OSV still returned no package-scoped records, so no forced page change was made. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public evidence.
## [2026-05-14] advisory-review | merge (npm)
Ran a public-information-only refresh against npm `merge` using OSV.dev package and vulnerability records plus the GitHub Advisory API collected under `raw/advisory-review-20260514-1806/`. Updated the existing `merge` page to add the now-confirmed CVE aliases for its two already-documented high-severity prototype-pollution advisories: `CVE-2018-16469` / `GHSA-f9cm-qmx5-m98h` fixed in `1.2.1`, and `CVE-2020-28499` / `GHSA-7wpw-2hjm-89gp` fixed in `2.1.1`. Also updated npm and master index descriptors. The required local Claude-compatible proxy synthesis step succeeded via `http://127.0.0.1:8319` and was used only as a drafting aid; final edits were checked against saved public OSV / GHSA evidence.

