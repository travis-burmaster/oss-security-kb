# Advisory review targets

Primary target: Linux `sudo` page gap from `wiki/linux/index.md` Future Targets.
Screened targets: Linux `openssh`, Homebrew `curl` future targets; `sudo` selected because upstream Sudo publishes concise public advisories for recent 2025 privilege-boundary CVEs plus canonical older Baron Samedit coverage.

Public sources only: upstream Sudo advisories / stable release notes, GitHub Advisory Database, OSV.dev, NVD/CVE records where reachable.
